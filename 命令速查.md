# 命令速查（historical_story_gen）

工作目录：`D:\Resources\code\historical_story_gen`
入口：`run.bat`（Windows 双击/命令行都可以），或 `.venv\Scripts\python.exe run.py`

> 如果从 Hermes 的终端里跑，前面要加 `env -u PYTHONHOME -u UV_INTERNAL__PYTHONHOME`
> （uv 注入的 PYTHONHOME 会让别的解释器崩在 `SRE module mismatch`）。
> 用 `run.bat` 则不用管，脚本里已经把这两个变量清掉了。

---

## 最常用的四条
```bat
run.bat                                   :: 随机题材，出横屏+竖屏
run.bat -t "赤壁之战：一场大火如何改写三国"   :: 指定题材
run.bat --minutes 9                       :: 目标 9 分钟（默认 7）
run.bat plan -t "安史之乱"                  :: 只写稿，不出语音/视频（省钱，几毛钱）
```

---

## 生成类

| 命令 | 作用 |
|---|---|
| `run.bat` | 随机选题，全流程，横竖两版 |
| `run.bat -t "主题"` | 指定题材 |
| `run.bat --seed 7 -t ...` | 固定随机种子（复现同一题材/同一套分镜） |
| `run.bat --minutes 8.5` | 目标总时长（分钟）。区间会自动设成 ±25% |
| `run.bat --chapters 8 --seconds-per-chapter 60` | 直接控制章节数与每章秒数（**时长主要靠这两个旋钮**） |
| `run.bat --seconds-per-scene 18` | 每个分镜秒数（越小画面切换越勤） |
| `run.bat -o portrait` | 只出竖屏；`-o landscape` 只出横屏；默认 `both` |
| `run.bat --no-images` | 不下载配图（全用渐变底图） |
| `run.bat --no-video` | 只到语音为止，不渲染视频（快速看稿+听语音） |
| `run.bat --no-bgm` | 不混背景音乐（默认**开**：`bgm.enabled=true`，音量 0.08、开场白念完才淡入） |
| `run.bat --allow-duplicate -t "..."` | 允许重做已经生成过的题材（默认会被拦下） |
| `run.bat --license-policy mixed` | 临时允许「关键词检索兜底」（图版权未明）。默认 `clean`：只用 CC0/公共领域/免费商用图库 |
| `run.bat --tts-provider edge` | 用免费 edge-tts 音色（不走 MiniMax）。MiniMax 欠费/限流时验证链路用 |
| `run.bat --force` | 忽略缓存重跑（配图/语音缓存都重做） |
| `run.bat --voice audiobook_male_1 --speed 1.1` | 换音色/语速（当前：系统音色 audiobook_male_1 @1.1）。**改完必须用 `python scripts\calib_rate.py` 重校 `story.chars_per_second`** |
| `run.bat --log-level DEBUG` | 打全量日志（排查用） |
| `run.bat plan` | 素材 → 大纲 → 写稿 → 校验，输出稿件 md + metadata.json |

---

## 探测 / 诊断类

| 命令 | 作用 |
|---|---|
| `run.bat probe-tts` | 合成一段 227 字范文，实测「字/秒」，看个大概。⚠️ 这是**连续合成**，比流水线的分镜合成快一截，**不要直接拿它当 `story.chars_per_second`**（系统音色：范文 4.77 / 流水线实际 4.59） |
| `run.bat probe-images` | 列图源可用性：谁参与、谁缺 key、谁被版权策略挡住、各返回多少候选 |
| `run.bat probe-images --download` | **实测下载速度**。判据是「下得动」不是「搜得到」—— 实测 met 只有 20 KB/s、artic 图床 403，光看候选条数发现不了 |
| `run.bat probe-images --query "Ming dynasty painting"` | 用指定检索词试图源 |
| `python scripts\probe_clean_images.py` | 零成本专项探测：clean 版权策略下 6 个典型分镜的**命中率**（不调 LLM/TTS）。实测 6/6 |
| `run.bat voices` | 列出 MiniMax 可用音色（确认 voice_id 拼写） |
| `python scripts\clone_voice.py --check` | **零成本**校验 `data\base_voice` 下的参考音频是否符合克隆门槛（10s–5min、≤20MB、mp3/m4a/wav） |
| `python scripts\clone_voice.py --voice-id hsg_story_v2` | 用参考音频克隆音色（MiniMax voice_clone），拿到可长期复用的 voice_id；填进 `config.yaml` 的 `tts.minimax.voice_id` |
| `python scripts\clone_voice.py --list` | 列出账号下已有的克隆音色 |
| `python scripts\tts_preview.py --script <脚本.md> --voice A --voice B` | **试听**：同一篇稿子用多个音色各出一条 mp3 做 A/B（不跑 LLM/配图/渲染）。加 `--announce` 让每段先念音色名 |
| `python scripts\calib_rate.py` | **0 成本**从已生成期的逐分镜时长反算流水线真实字/秒 —— 换音色/改语速后校准 `story.chars_per_second` 的正确做法。加 `--last 3` / `--voice <id>` 过滤 |
| `run.bat history` | 看生成记录：已做过哪些故事、多长、配图命中多少、校验情况 |
| `run.bat history --backfill` | 把 `data\output` 下已有的 metadata 补录进生成记录 |
| `run.bat test` | 零成本回归测试：清洗 / 校验 / 复检 / 版权策略 / 查重 / 分镜编号 / 语速传导 / 记录写入 共 189 项，不调 API |
| `python scripts\audit_config.py` | **配置审计**：找出「写在 config.yaml 但代码从不读」的假开关（已清过一轮，当前 0 项），以及反向的「代码会读但配置没写」 |
| `run.bat smoke` | **零 LLM** 跑通「配图→画面→字幕→编码→拼接」，媒体层出问题先跑这个 |
| `python scripts\smoke_video.py --no-tts` | 同上，且用静音轨（完全不花钱） |
| `python scripts\rerender.py` | 从已有 metadata 重渲染：**复用文稿和语音**，只重做配图与画面。<br>改配图策略、版面、编码参数时用它，不用重新花 LLM/TTS 的钱 |
| `python scripts\rerender.py --no-images -o portrait` | 只重出竖屏、且沿用已有配图 |
| `python scripts\check_layout.py frame.png` | 量一帧里标题带和字幕带是否重叠（程序化判定，不靠肉眼） |

---

## 关键配置（config.yaml）

| 位置 | 说明 |
|---|---|
| `story.chapters` / `story.seconds_per_chapter` | **时长主旋钮**。总时长 ≈ 片头 + 章节数 × 每章秒数 |
| `story.angle_mode` | 切入方式：`small` 小切口（默认，从一个日常问题进） / `event` 事件式。四处提示词一起切 |
| `verify.fact_audit` | 写稿前的锚点核查（把编造的出处挡掉）。`true` 默认开 |
| `verify.block_on_fail` | **复检闸门**。改写后仍有必修问题时：`false`（默认）照常出片但写进「待人工核对」清单；`true` 直接中止（退出码 3），有问题就不出片 |
| `images.license_policy` | `clean`（默认）只用版权明确的图库 / `mixed` 允许关键词检索兜底（版权未明） |
| `images.providers` | 用哪些图源。默认 `["cleveland","pexels","unsplash"]`（后两个要 key，没有就自动跳过）。**实测只有 Cleveland 真能下得动** |
| `images.culture_keywords` | 文化圈过滤词表（默认中国相关）。讲日本/欧洲故事时改这里 |
| `images.fallback_queries_en` | 兜底英文检索词（「时代 + 泛画种」），分镜自己的词太具体时救场 |
| `verify.llm_audit` | 写稿后的史实审校（年份/人名/数字/因果） |
| `story.target_total_seconds` / `min` / `max` | 目标与硬性区间（默认 420s / 300-600s）。超区间会自适应改写 |
| `story.seconds_per_scene` | 分镜秒数 → 决定配图张数与画面节奏 |
| `story.chars_per_second` | 语速换算，`probe-tts` 实测得来。**当前值属于旧音色，待用克隆音色重测** |
| `llm.provider` | **已锁定 deepseek**，改这里会被启动校验拦下报错 |
| `tts.minimax.voice_id` / `speed` | 音色与语速（当前：克隆音色 `hsg_story_v2` / `1.1`） |
| `video.intro` / `video.outro` | 片头/片尾画面开关（键名是 `outro`，曾叫 `outro_card` 那个名字代码不读，已改） |
| `images.drop_page_keywords` | 旅游站/素材站/AI 图库/新闻门户降权词（避免水印与版权风险） |
| `images.show_credit` | 画面上是否显示图源出处（默认 false） |
| `video.motion` / `motion_scale` | 画面缓移（默认开）与背景放大倍率 |
| `video.preset` / `crf` | 编码质量。medium 一段 20 秒约 10 秒编完 |
| `subtitle.margin_v_ratio` | 字幕垂直位置，改这里要同步看 `media.build_layers` 的字幕区留白 |
| `bgm.enabled` | 背景音乐，默认关 |

---

## 音色克隆（voice_clone）

```bat
python scripts\clone_voice.py --check                      :: 只校验参考音频，不调接口
python scripts\clone_voice.py --voice-id hsg_story_v2      :: 克隆（参考音频默认取 data\base_voice 下最新一个）
python scripts\clone_voice.py --list                       :: 列出账号下已有的克隆音色
python scripts\tts_preview.py --script data\output\x_脚本.md --voice hsg_story_v2 --announce
```

参考音频门槛：**10s ~ 5min、≤20MB、mp3/m4a/wav**、单人干声（无背景音乐最好）。
克隆出来的音色挂在 MiniMax 账号下，换 key 就没了；voice_id 在同一账号内必须唯一。

两条反直觉的实测行为（都在 `scripts\clone_voice.py` 里注释固化了）：

| 现象 | 真相 |
|---|---|
| 上传返回的 `file_id` 传字符串 → 报 **2013 invalid params** | `file_id` 必须传**整数**。报错信息完全指不到参数类型上 |
| `get_voice` 的 `voice_cloning` 列表里**看不到**刚克隆的音色 | 列表不可信。判断克隆成功要**拿 voice_id 直接合成**，出声才算成 |

换音色之后的必做顺序：

```bat
run.bat probe-tts        :: 1. 实测新音色的「字/秒」
:: 2. 把实测值填进 config.yaml 的 story.chars_per_second
run.bat                  :: 3. 出片
```

> ⚠️ `probe-tts` 有兜底守卫：如果 MiniMax 余额耗尽/限流导致实际是 edge 免费音色念的，
> 它会明确拦住并返回退出码 5，**不给出**校准建议 —— 拿另一个音色的语速去校准，
> 比不校准更糟。同样，`synthesize_all` 结束时若有分镜走了兜底，会打印
> 「有 N/M 段用了 edge 兜底音色」的告警：成片配音已经不是你配置的那个人了。

---

## 生成记录（做过什么，选题去重的依据）

```bat
run.bat history              :: 列出已生成的期目（时间/标题/成片时长/配音时长/分镜/配图）
run.bat history --backfill   :: 把 data\output 下已有的 metadata 补录进来
type data\生成记录.md          :: 直接看人读版（表格 + 每期明细）
```

记录文件：`data\history.json`（机器读、查重依据）+ `data\生成记录.md`（人读）。

去重行为：
- 不带 `-t` 自动选题 → 跳过做过的题材；池子挑空了让模型出新题
- 带 `-t` 指定做过的题材 → **拦下，退出码 2**，并提示怎么办
- 换了个说法也算重复（规则判不出的会交给 DeepSeek 裁定一次）
- 确实要重做 → 加 `--allow-duplicate`

---

## 出问题先看这里

| 现象 | 处理 |
|---|---|
| 成片没声音 | 系统缺 AAC 解码器（Windows N 版常见）。把 `video.audio_codec` 改成 `libmp3lame` 或 `ac3`，重跑该朝向即可 |
| 时长不在 5-10 分钟 | 流水线会自动改写拉回区间；若仍超出，调 `story.chapters` / `seconds_per_chapter`，或直接 `--minutes` |
| 某个分镜没配图 | 看 `data\images\_sources.json`（每个分镜试了哪些检索词、各返回多少候选、最终选了哪张）。该分镜会用渐变底图，不影响成片 |
| 配图年代不对/有现代元素（电线杆、景区照） | 关键词检索无法保证年代准确。已做「古画/文物向优先 + 旅游素材站降权」，但仍可能出错。要版权与年代都靠谱：`images.providers: ["artic","met"]`；要精确控制就人工替换 `data\images\scene_XXX.jpg` 后重跑渲染 |
| 图源全挂 | `run.bat probe-images` 确认；必要时把 `images.providers` 只留 `["artic","met"]`（博物馆开放接口，公共领域） |
| 字幕和语音不同步 | 无需担心 —— 一个字幕/一段语音就是同一个分镜，且音频缓存文件名带文本哈希，改写后会重新合成 |
| 想换成别的文本模型 | 本项目**硬性锁定 DeepSeek**：要改必须同时改 `src/hsg/config.py` 的 `TEXT_PROVIDER_LOCK` 和 `config.yaml` 的 `llm.provider` |

---

## 产物位置

```
data/output/20260911_<标题>_横屏.mp4     成片（横屏）
data/output/20260911_<标题>_竖屏.mp4     成片（竖屏）
data/output/20260911_<标题>_脚本.md      口播稿 + 章节结构 + 配图出处
data/output/20260911_<标题>_metadata.json 全量元数据（分镜/时长/配图/时间轴）
data/material/                           检索到的素材（缓存）
data/audio/                              每个分镜的语音
data/images/                             配图 + _sources.json
data/slides/<朝向>/                      合成的画面（bg=配图层 / fg=文字层）
data/segments/<朝向>/                    分镜片段 + _concat_*.mp4（拼接母版）
data/tmp/smoke/                          冒烟测试产物
```
