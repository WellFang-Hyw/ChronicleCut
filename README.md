# historical_story_gen · 历史小故事视频生成

给一个历史题材（或者不给我），跑出一条 5-10 分钟的视频。有**两条产出路线**：

**路线 A · 静态图（默认，一条命令到底）**

```
选素材 → AI 出分章大纲 → AI 逐章写口播稿（切成「分镜」）
      → 史实校验（程序化 + LLM 审校，不合格带问题重写）
      → TTS 配音 → 找历史配图 → Pillow 合成画面 + ASS 字幕
      → ffmpeg 编码分镜 → 拼接 → 横屏 / 竖屏两版成片
```

**路线 B · 影视切片（历史解说视频形态：人工剪素材 + AI 剪辑）**

```
run.bat agent --stage 1   → 脚本 + 分镜 + 素材需求清单（出完就停下，这是闸门）
        ↓ 人工：按清单剪 6-10 秒片段 → python scripts\import_clip.py 绑槽位
run.bat agent --stage 2   → AI 导演排镜头(EDL) → 按镜头渲染 → 自检 → 双版成片
                            画面 = 影视切片 + 定格放大 + 大字标注 + 人名条
```

两条路线共用同一套画面层（排版、字幕、BGM、封面、拼接都是同一份代码），
所以 B 路线里没找到素材的分镜会**自动回退**成 A 路线的静态图画面，不会卡住出片。
架构与分工见 `docs/Agent应用架构.md`。

一句话概括技术选择：**文本用 DeepSeek，语音用 MiniMax，画面用 Pillow 离线合成，
运动与编码交给 ffmpeg**。

> 🤖 让 AI agent 在本项目干活前，先读 **`AGENTS.md`** —— 那里写了硬性约定
> （文本模型锁、配音固定用哪个音色和语速、改代码的护栏、已知机制局限）。

---

## 1. 快速开始

依赖已经装好了（`.venv` + 项目已 editable 安装）。

```bat
cd /d D:\Resources\code\historical_story_gen

run.bat                                           :: 随机题材，出横竖两版
run.bat -t "赤壁之战：一场大火如何改写三国"          :: 指定题材
run.bat --minutes 9                               :: 目标 9 分钟
run.bat plan -t "安史之乱"                          :: 只写稿（几毛钱，不出视频）

:: 影视切片路线（解说视频形态）
run.bat agent                                     :: 看生产线卡在哪、下一步干什么
run.bat agent --stage 1 -t "曹操为何杀吕伯奢"        :: 出脚本 + 素材需求清单，然后停下
run.bat agent --stage 2                           :: 素材到齐后：AI 排镜头 → 渲染 → 自检
run.bat smoke-clips                               :: 零 API 冒烟：切片 + EDL 剪辑链路

:: 选题（两级：故事类型 + 标题与描述）
run.bat topics                                    :: 看整个选题池（含系列条目）
run.bat series "古代十大权臣"                       :: 看系列进度 + 下一集是哪一集
run.bat topics --type "刑狱与流放"                  :: 只看某一类
run.bat --type "行旅与驿传"                         :: 只在某一类里挑题
```

不想用批处理也可以：

```bash
.venv/Scripts/python.exe run.py -t "鸿门宴上，项羽为什么没杀刘邦"
```

> 从 Hermes 这类会注入 uv `PYTHONHOME` 的终端里跑，前面加
> `env -u PYTHONHOME -u UV_INTERNAL__PYTHONHOME`；`run.bat` 里已经清掉了。
>
> 完整命令表见 `命令速查.md`。

---

## 2. 目录结构

```
historical_story_gen/
├── AGENTS.md              给 AI agent 的项目约定（硬性约定/护栏/已知局限）← 先读这个
├── config.yaml            所有可调参数（时长、音色、图源、字幕、编码…）
├── README.md / 命令速查.md 说明与命令表
├── run.bat / run.py       入口
├── docs/
│   ├── Agent应用架构.md    切片路线的三层分工（AI 决策 / 代码执行 / 证据）与阶段闸门
│   └── plans/             实现计划（历史解说视频 v1：影视切片 + 剪辑标注）
├── src/hsg/
│   ├── config.py          配置加载 + 密钥三路兜底 + 文本模型锁
│   ├── models.py          Story / Chapter / Scene 数据结构
│   ├── textfmt.py         口播清洗（去 markdown/来源词、日期不补零、折行修正）
│   ├── llm.py             OpenAI 兼容的 LLM 客户端（JSON 模式 + 截断自动重试）
│   ├── material.py        素材检索（Bing 摘要，作人名/年份锚点）
│   ├── topics.py          选题池（两级：故事类型 + 标题与描述，可按类型挑）
│   ├── outline.py         阶段 A：分章大纲
│   ├── writer.py          阶段 B：逐章写稿 + 长度自适应扩写/精简
│   ├── verify.py          史实校验（规则层 + LLM 审校 + 锚点核查 + 复检闸门）
│   ├── tts.py             MiniMax T2A（+ edge-tts 兜底）
│   ├── images.py          配图：版权安全图库优先（Cleveland CC0 等）+ 年代匹配 + 去重
│   ├── clips.py           影视切片素材库：索引 / 多维检索 / 规范化导入 / 合规配额
│   ├── needs.py           素材需求清单：分镜 → 槽位（「要剪什么片段」的工作单）
│   ├── edl.py             剪辑表（EDL）：AI 导演排镜头 + 合规校验 + 素材出处
│   ├── frames.py          抽帧 / 定格放大 / 标注位置按像素验收
│   ├── shotvideo.py       镜头编码 + 多镜头拼接 + 贴回语音（护栏 12 的落地点）
│   ├── agent.py           阶段闸门编排：stage 1 停下等素材 / stage 2 自动出片
│   ├── subtitles.py       ASS 字幕（按分镜时长铺时间轴）
│   ├── media.py           Pillow 合成画面（背景层 + 前景层，两块分开出图）
│   ├── video.py           ffmpeg：编码片段 / 拼接 / 混 BGM / 读回参数
│   ├── pipeline.py        编排 + 长度控制 + 产物输出
│   └── cli.py             命令行
├── scripts/
│   ├── smoke_video.py     零 LLM 的媒体链路冒烟测试 ← 媒体出问题先跑它
│   ├── test_verify.py     零成本回归测试（清洗/校验/复检/版权策略/查重/分镜编号/语速传导/切片/需求清单/剪辑表/标注/两级选题/系列/开场白/封面风格，520 项）
│   ├── probe_clean_images.py  零成本配图专项探测（clean 策略下的命中率/版权/年代）
│   ├── rerender.py        从已有 metadata 重渲染（复用文稿和语音，只换配图/画面）
│   ├── check_layout.py    量一帧里标题带和字幕带是否重叠（不靠肉眼）
│   ├── smoke_clips.py     零 API 冒烟：切片 + EDL 剪辑链路（合成假素材钉死全链路）
│   ├── make_needs.py      出素材需求清单（脚本 → 槽位工作单；脚本先行那一环）
│   ├── import_clip.py     导入影视切片（规范化 + 去音轨 + 登记 + 绑槽位）
│   ├── calib_rate.py      从已生成期反算真实字/秒（校准 chars_per_second，别用 probe-tts）
│   ├── clone_voice.py     用参考音频克隆音色（MiniMax voice_clone → 可复用的 voice_id）
│   ├── tts_preview.py     多音色 A/B 试听（同一篇稿子各出一条 mp3，不跑 LLM/配图/渲染）
│   └── audit_config.py    配置审计（找出代码从不读的假开关 / 配置里没写的键）
└── data/                  产物（见「产物位置」）
```

---

## 3. 每个阶段在做什么、为什么这么做

### 3.1 素材（material.py）
拿 Bing 检索摘要当「人名/年份锚点」。**不是**让模型照抄，而是给它一个参照，
写稿仍以模型自身知识为主，再靠 `verify.py` 兜住事实。

为什么不用维基百科 —— 实测（2026-09，国内网络）：

| 来源 | 实测结果 |
|---|---|
| `zh.wikipedia.org` / `commons.wikimedia.org` | **直连**连接超时；**开代理/TUN 后可用**（实测 2026-09-15） |
| `baike.baidu.com` | HTTP 403（人机校验） |
| `cn.bing.com`（网页/图片检索） | **可用** ← 采用 |
| `api.artic.edu`（芝加哥艺术博物馆开放接口） | 可用，公共领域 |
| `collectionapi.metmuseum.org`（大都会 CC0） | 可用 |
| `openaccess-api.clevelandart.org` | 超时 |

### 3.2 写稿：为什么要分成两次 LLM 调用
- **阶段 A（大纲）**：一次调用只出结构（章节标题、要点、目标秒数、配图检索词）。
- **阶段 B（写稿）**：每章一次调用，返回若干「分镜」，每段 70-120 字。
- 好处：输出短、不易截断；6 章可以 **4 路并发**（实测比单次大调用快得多）；
  长度控制有地方下手（章节数 / 每章秒数）。

### 3.2b 切入方式：小切口（默认）还是事件式

`config.yaml` 的 `story.angle_mode` 决定整期的内容视角 —— 选题池、大纲、写稿、审校
四处提示词会一起切过去：

| 模式 | 讲法 | 例子 |
|---|---|---|
| `small`（默认） | 从一个具体的日常问题切进去，再用史实把答案讲成故事 | 「流放三千里：被流放的人一路上靠什么活下来？」<br>「城破那一天：官员、商人、僧人、工匠各自的结局」<br>「官员出差：从京城到广州，走多久、住哪儿、路上花多少钱？」 |
| `event` | 直接讲一场战役 / 一次变法的来龙去脉（旧行为） | 「赤壁之战：一场大火改写了三国」 |

小切口模式有三个关键设计，**每一个都是实跑撞出来的**：

1. **大纲必须先给「史实锚点」（facts）** —— 每章至少一条可考事实；
   写不出来就说明这个角度立不住，要换角度。
   起因：第一版让模型自己回答「一天走多少里、一天几文」，它为了答得漂亮，
   直接编了「每天走五十里」「米一升折银五厘」还说成《大清会典》的规定 ——
   审校一次抓出 8 条编造的具体数字。
2. **写稿前有「锚点核查」闸门**（`verify.audit_facts`，由 `verify.fact_audit` 控制）：
   逐条判断锚点 ok / unsure / wrong —— ok 保留、unsure 降级标注、wrong 删除。
   降级后的锚点写稿时只能用模糊说法（「史料记载不一 / 没有明确规定」），
   不许引出处和数字。
   判定标准要分清两类：**制度层面的通行描述（学界共识）判 ok，只有精确数字
   和逐字引用才要求出处落实**。第一版标准过严，18 条锚点全判存疑，
   内容被抽空 —— 严不等于好，会把节目变成一堆「史料没记载」。
3. **写稿只许用已确认的锚点**，并且明确允许「没有记载」本身就是答案。
   实测这句话很管用：稿子里会出现「律例里并没有规定一天要走多少里，
   实际快慢全看路况、天气和押解的差役」—— 可信度和信息量都高于一个编出来的数字。

四轮 prompt 迭代的效果（同一题材）：

| 轮次 | 结果 |
|---|---|
| 只有小切口 prompt | 审校抓出 **8 条编造数字**（还都声称出自某会典） |
| + 大纲 facts 锚点 + 写稿只许用锚点 | 降到 **2 条**真错（年代张冠李戴、里程与实际不符），不再有编造出处 |
| + 锚点核查（标准过严） | 18 条全判存疑 → 内容被抽空 |
| + 核查标准分两类 | 确认 8 / 存疑降级 10 / 删除 0，成稿后审校 **0 条必修** |

### 3.3 分镜（Scene）是整个流水线的原子单位
一个分镜 = 一段口播 + 一张配图 + 一条字幕 + 一段语音。

这么切的原因：如果一章 68 秒只配一张图会显得很呆；而如果整章合成一段语音再
按时间切开对齐画面，切口很容易切在字中间。按分镜合成则天然是
「一段文字 = 一段语音 = 一张图 = 一片字幕」，边界严丝合缝，字幕和语音不可能错位。

配套的一个正确性细节：音频缓存的文件名是 `scene_003_<文本哈希>.mp3`。
只按分镜号命名的话，改写后（分镜号没变、文字变了）会复用到旧语音，
字幕和声音就对不上了。

### 3.4 史实校验（verify.py）—— 这是本项目最值钱的一环
两层：

**规则层（零成本）**：空/过短/过长分镜、跨分镜重复句子、占位符残留、
markdown 残留、清洗后仍出现的「百科/维基/百度」等来源词、
年份是否落在故事声明的年代区间附近。

**LLM 审校层**：让模型当「史实校对员」，对照年代区间与素材逐条挑错，
只报能确证的问题（宁可漏报不要误报）。命中的分镜带着「错在哪、应该怎么改」
重写一次。

实测效果（第一次真跑「赤壁之战」，18 个分镜）：审校抓出 **10 条真实错误**，
包括

- 「刘备南撤到江夏」→ 应为夏口（且与后文自相矛盾）
- 「周瑜任左都督、程普任右都督」→ 东吴当时是**左督/右督**，「都督」是后世官称
- 「孙刘联军逆江而上」→ 方向说反（联军是溯江西上迎击，曹军才是顺江东下）
- 「三国鼎立从此拉开」→ 赤壁之后只是**三分雏形**，鼎立正式形成于 229 年孙权称帝
- 「火攻后周瑜立刻拿下南郡」→ 南郡之战打到 209 年，时间线不准

这类错误提示词约束根本拦不住，必须靠一遍独立的审校。

**复检闸门（verify.final_check）**：审校发现问题 → 带纠正意见重写章节 →
**重写的结果必须再验一遍**（规则层 + 审校层各跑一次）。没有这一步，
「修没修好」全靠猜。

- 复检通过 → 正常进 TTS。
- 复检仍有必修问题 → 清单写进稿件的「⚠️ 待人工核对」一节 + metadata 的
  `unresolved`，日志显著提示；
  `verify.block_on_fail: true` 时直接中止（退出码 3），**有问题就不出片**。

实测：审校抓 10 条 → 改写后复检 0 条必修（鸿门宴）；城破那天抓 3 条真错 →
复检归零。没归零的会以清单形式交到人手上，不会被静默忽略。

### 3.5 配图（images.py）—— 版权安全优先

**第一原则是版权**（防止视觉中国式索赔），所以默认 `images.license_policy: clean`：
只用版权明确的图库。

一个分镜找图的**完整顺序**（这是现在的实际行为，别只记住图库那一段）：

```
① 生成（images.generate: true，模型 image-01）  ← 拿到就用，直接结束
② 图库检索（原检索词）                          ← 生成失败/关掉时才走
③ 图库检索（兜底检索词：时代 + 泛画种）          ← 实测能把「馆里没有具体题材」救回来
④ 渐变底图（fallback_bg）                       ← 都没有就出一张底色图，不阻断出片
```

生成优先的理由：**不受图库索引限制**（冷门题材馆里根本没有对应的图），
而且**没有第三方版权**。代价是人物长相跨镜头不一致，以及偶发伪汉字（下面有护栏）。

| 图源 | 版权 | 需要 key | 特点 |
|---|---|---|---|
| `cleveland` | **CC0 公共领域** | 不需要 | 克利夫兰艺术博物馆。带 `creation_date`，**能按年代匹配**；中国绘画收藏好，图片两三 MB，下载快 |
| `artic` | **公共领域** | 不需要 | 芝加哥艺术博物馆。带 date_start/date_end |
| `met` | **CC0** | 不需要 | 大都会艺术博物馆。可限定「亚洲艺术部」，但原图常有十几 MB，排在后面 |
| `unsplash` | Unsplash License（免费商用、免署名） | `UNSPLASH_ACCESS_KEY` | 现代摄影，历史题材基本用不上 |
| `pexels` | Pexels License（免费商用、免署名） | `PEXELS_API_KEY` | 同上 |
| `si` | CC0 | `SMITHSONIAN_API_KEY` | Smithsonian Open Access |
| `rijks` | 公共领域 | `RIJKSMUSEUM_API_KEY` | 荷兰国立博物馆 |
| `bing` | **版权未明** | 不需要 | 全网关键词检索。**只在 `mixed` 策略下参与兜底** |

没配 key 的图源会自动跳过，不影响运行 —— 缺 key 不会让流程没图可用。

**实测（2026-09，国内网络直连）**：无 key 且**真的下得动**的公共领域源，目前只剩
`cleveland` 一家（met 的图服务器只有 20 KB/s，artic 的图床一律 403 —— 搜得到但下不动）。
Openverse 连接超时、美国国会图书馆被 Cloudflare 拦（403）、
Wikimedia Commons **直连**不可达（开代理后可用，而且是切片/叠加料的首选源 —— 上万条 PD/CC 素材）、
Rijksmuseum 旧 demo key 已失效。所以要扩充图源，最实际的是去领 Unsplash / Pexels
的免费 key（注册即得），填进环境变量即可，代码不用改。

**生成配图（image-01）的三个要点**：

1. **风格后缀必须按分镜类型分派**（`classify_scene_kinds`，规则兜底 + 一次便宜 LLM 覆盖）：
   器物分镜用「器物向后缀」（整幅画面只有器物本身），叙事分镜用「场景向后缀」
   （人物与场景为主体）。不分派的话器物后缀会把叙事分镜也压成静物小品 ——
   第 7 期实测：要找坊市布局图给了干裂土地、要找巡夜兵丁给了灯笼、
   要找衙门审案给了**一把西式法槌**。
2. 两条后缀都**不许**出现「工笔/绢本」（会诱发伪书法题跋 + 红印章）和「摄影」
   （会诱发图库水印）。这两个坑各踩过一轮。
3. ⚠️ **控制台里的显示名（如「H3」）API 不认**，配置里必须写模型名 `image-01`
   （文生图）/ `image-01-live`（图生图）。改配置时别照抄控制台。

选图顺序（**实跑调出来的**，仅在走图库那条路时生效）：

1. **年代贴合** —— 博物馆接口带年代字段，明末的故事优先选明代作品（陈洪绶 1598–1652
   这种），宋代山水差 470 年就往后排。这是用博物馆图源换来的额外好处。
2. **馆藏来源**（`prefer_page_keywords`：博物馆/档案馆/美术馆/古籍库）优先。
3. **美术标记**（画/卷/壁画/绢本/青铜/陶/俑…）优先。
4. **版权干净** 优先于版权未明。
5. **图源顺序**（同样贴合时克利夫兰/芝加哥排在 Met 前面，因为它们给的图小、下得快）。

两条必须的护栏：

- **文化圈过滤（`culture_filter`）**：博物馆接口是关键词匹配，版权干净也可能**离题** ——
  实测搜 `Ming dynasty painting city wall` 时，芝加哥艺术馆返回了《大碗岛的星期天》和
  伦敦议会大厦，大都会返回了波斯手抄本。讲明代城破的片子里放一张莫奈，比放现代照片
  还糟。所以默认只保留中国相关作品（`culture_keywords`），讲日本/欧洲故事时改这个词表。
- **单图体积上限（`max_mb: 25`）**：博物馆原图有几十 MB 的，画布最多 4K，超限直接跳过，
  否则一张图就能把流程卡住几分钟。

每条记录里都带 `round` 字段标明它是「生成」还是第几轮图库检索来的，
所以事后能看出这张图到底怎么来的。生成图的出处标成它的 `generator`（如 image-01）。

每张图选了哪张、什么许可证、什么出处，都写进 `data/images/_sources.json`
与稿件末尾的「配图出处与版权」一节 —— 公开被质询时这就是凭据。
版权未明的图会被显式标成 `⚠️版权未明`，不会混在干净图里看不出来。

想临时用回关键词检索（赶时间/自己判断风险）：`run.bat -t "主题" --license-policy mixed`，
或把 config 的 `license_policy` 改成 `mixed`。

背景：实跑搜「函谷关遗址 古关隘」拿到的是现代景区照片，画面里**有电线杆**，
历史沉浸感当场破功。上面这套排序是为此加的。

配套的质量措施：
- 分辨率门槛（默认 ≥700×480）、长宽比门槛、明显不适合当背景的 URL 直接跳过；
- **平均哈希去重**：同一张图不会在片子里出现两次；
- 每个分镜的检索过程写进 `data/images/_sources.json`（试了哪些词、多少候选、
  最终选了哪张、来自哪一组），没图时好排查。

**⚠️ 诚实说明这条链路的边界**：关键词检索没法保证「年代准确」。
排序优化能去掉大部分现代元素（景区照、素材图），但仍可能选到年代不对的历史照片
（实测秦汉语境下选到过一张民国时期的老照片）。要真正解决只有两条路：
① 把图源限制成 `["artic","met"]`（博物馆开放接口，公共领域、年代靠谱，但命中率低）；
② 接一个视觉模型做配图复核（需要视觉模型 key，当前项目没有，属可选扩展）。

博物馆接口的图是公共领域，Bing 搜来的图版权不明 —— 每个分镜的出处都会写进
稿件的「配图出处」段和 `metadata.json`。`images.show_credit: true` 可以把出处
显示在画面上（默认关）。要公开发布建议走 `artic`/`met` 那条路。

### 3.6 画面（media.py + video.py）
Pillow 出**两层**图，运动交给 ffmpeg：

- `bg.jpg` —— 配图按 `motion_scale`(1.16) 放大后铺满 + 压暗，**这层在 ffmpeg 里缓移**
- `fg.png` —— 透明层：上下压暗渐变 + 栏目名/章节标题/图注，**这层不动**

这样配图缓慢横移时文字纹丝不动，比整帧缩放耐看得多；运动用 `crop` 的
时间表达式实现（比 `zoompan` 便宜得多，20 秒片段只编 10 秒）。

**路线 B 的多镜头也走同一条链**：切片镜头是「背景层换成视频 + 标注层不变」，
定格放大则是「抽一帧放大后当静态图」，所以排版、字幕、BGM、封面、拼接全是同一份代码
（详见 §3.8）。

分段编码 + `concat -c copy` 拼接。转场是「淡黑」而不是 `xfade` ——
`xfade` 要全片重编码且滤镜链脆弱，淡黑在片段编码阶段就完成了，拼接是秒级的。

### 3.7 长度控制（pipeline.fit_length）
需求是 5-10 分钟，所以设了硬性区间 `[300s, 600s]`：

1. TTS 完之后立刻量总时长；
2. 在区间内 → 直接过（实测第一次就跑出 406.7s = 6.78 分钟）；
3. 超区间 → 按比例算出每章目标，挑偏差最大的章节带指令改写
   （扩写要求「补细节/场景/因果」，精简要求「砍重复/铺陈」），只重合成被改写的章节；
   最多两轮。

即临时长仍超出会明确告警，并指出该调哪个旋钮（`story.chapters` /
`seconds_per_chapter`）—— 不会静默给一条 4 分钟或 18 分钟的片子。

---

### 3.8 影视切片路线（解说视频形态）

静态图路线的问题是「一个 20 秒分镜就一张不动的图」。切片路线把画面换成**影视片段**，
再叠剪辑手法 —— 代价是素材得人工剪（v1 不做自动视频索引，那是一个独立工程）。

> 📖 **每期照着走的操作手册（命令 + 判据 + 排查表）见 [`docs/生产线操作手册.md`](docs/生产线操作手册.md)**；
> 下面这节讲的是**为什么这么设计**。

#### 完整流程（从定选题到出片，五步）

```
第 0 步 · 定两级选题（三种方式，任选）
  ① 让池子挑：   run.bat topics                   先看有哪几路故事
                 run.bat --type "行旅与驿传"        只在某一类里挑
                 run.bat plan                    不带 -t：自动挑（自动避开做过的 + 避开最近做过的类型）
  ② 池子里挑好标题后按它跑：run.bat plan -t "<池子里的标题>"   （类型和描述池子自带）
  ③ 自己制定二级：run.bat plan -t "标题" --topic-type "行旅与驿传" --topic-desc "这一期到底讲什么"
                  · --topic-type 必须来自受控词表（写错当场报错并列出全部可选值）
                  · 不填这两项就由模型判类型、生成描述（各一次便宜调用）

第 1 步 · 出脚本 + 素材需求清单（然后它会停下）
  run.bat agent --stage 1 -t "标题" --topic-type "行旅与驿传" --topic-desc "…"
  → data/output/<日期>_plan_脚本.md / _metadata.json
  → data/needs/<日期>_<题材>_素材需求.md（照这份剪素材）

第 2 步 · 剪素材并导入（唯一必须人工的一步）
  python scripts\import_clip.py --file <你剪好的.mp4> --id sg_01 --title "三国演义" \
      --year 1994 --people 曹操 --era 东汉末 --slots s04_sh1,s04_sh2
  随时看进度：run.bat agent（它会告诉你还缺几条、先剪哪几条）

第 3 步 · 出片（全自动）
  run.bat agent --stage 2
  → AI 排镜头(EDL) → 按镜头渲染 → 自检 → 横竖双版 + 封面 + 素材出处清单

第 4 步 · 检查
  run.bat agent              看状态与产物路径
  data/edl/*_EDL.json        剪辑表（每镜用了哪条素材的哪一段、什么手法）
  data/output/*_素材出处.md   发布举证
```

> 只想出稿不想出片：把第 1 步换成 `run.bat plan -t "…" --topic-type … --topic-desc …`
> （`plan` 与 `agent --stage 1` 是同一件事，后者会额外去出素材需求清单）。

**为什么必须停**：剪素材是整条链里唯一必须人工、也最慢的一环。
先剪后配会剪一堆用不上的，所以清单必须排在剪素材**之前**单独产出。
**阶段 2 故意不重写字稿** —— 你已经照稿剪了素材，它再「顺手优化」就是背刺
（时长跑出区间只告警，改不改由你定）。

#### 素材规范（导入工具会强制校验）

| 项 | 要求 | 为什么 |
|---|---|---|
| 单段时长 | **≤ 10 秒**（`clips.max_seconds`） | 合规：短段更像「引用」，长段更像「搬运」 |
| 画面占比 | 切片总时长 **≤ 成片的 45%**（`clips.max_share`） | 同上，超了**直接拒绝出片** |
| 音轨 | **必须没有**（导入强制 `-an`，且入库前再验一次） | 不用原声（版权），我们有自己的配音 |
| 画幅 | 1920x1080 / 30fps（工具自动转，`clips.spec`） | 拼接要参数一致 |
| 来源字段 | `title`/`year`/`people` 必填 | 发布前出「素材出处清单」举证 |
| 字幕水印 | 不要 | 我们自己叠标注，双重文字很难看 |
| 源片自带硬字幕/台标 | 导入时用 `--crop-top` / `--crop-bottom` **裁掉** | 烧死在画面里，ffmpeg 去不掉，只能连画面一起裁（裁了多少会记进举证清单） |

#### 清单怎么读（两级粒度别混）

清单按**槽位**给需求（`s04_sh1` = 第 4 分镜第 1 段），槽位是按「单个片段」切的：
22 秒分镜、单段上限 10 秒 → 切 3 段。但**每个分镜只有第 1 段要真剪**，
第 2 段起标「↺ 复用本场素材」（用同一段素材的不同段落铺满）——
所以一期通常是「几段到十几段要剪」，而不是几十段。优先级（★必须）只落在**章节开场**。

#### AI 导演与剪辑手法

阶段 2 会调一次 LLM 当**剪辑指导**，为每个分镜排镜头序列（哪条素材、从第几秒取、
取多久、什么手法、画面上打什么字）。它只能用手头真有的素材，编出不存在的 id 会被拦下。

| 手法 | 怎么实现 | 什么时候用 |
|---|---|---|
| `plain` | 素材直铺（素材本身就在动） | 交代环境、过场 |
| `slow_push` | 画布放大后按时间表达式缓慢平移裁切 | 想强调、放慢节奏 |
| `freeze_zoom` | **抽一帧 → 裁块放大 → 当静态图走原来的静态图路径** | 旁白说到某个瞬间/细节 |
| `still_pan` | 静态图 + 缓移（没素材时的回退画面） | 找不到素材的分镜 |

`freeze_zoom` 那个取巧是全案能低成本落地的关键：**不写任何 zoompan 滤镜**，
直接复用已有的静态图编码链路，还白捡原有的缓动。

画面元素：**大字标注**（2-4 字，带底条，手机小屏一眼读完）+ **人名条**（观众不认识这张脸时给）。
版面分区固定、按像素验收（`frames.ink_band`），不许互相侵入。

竖屏放横屏素材默认 **blurpad**（完整画面居中 + 上下模糊衬底），不硬裁 ——
16:9 进 9:16 硬裁只剩中间约 32% 宽度，人物很容易被裁掉。

#### 出片前的闸门与举证

- 剪辑表要过 `edl.validate_edl`：单段 ≤ 10 秒（**只管切片**，静态图镜头不受限）、
  切片占比 ≤ 45%、镜头总长 = 画面时长、素材 id 在库、取用区间不越界。
- 出片后自检：文件在、时长覆盖正文、画幅正确、**有音轨**（没声音这类问题日志里看不出来）。
- 自动出一份 `data/output/<日期>_<标题>_素材出处.md`：每条素材的片名/年份/人物/
  时长占比/用在哪些分镜 —— 公开发布被质询时这就是凭据。

#### 零 API 冒烟

```
run.bat smoke-clips        横竖两版都跑，用 ffmpeg 合成的假素材把整条剪辑链路钉死（34 项）
python scripts\smoke_clips.py -o landscape   :: 只跑一版，快一倍
```

素材库用**独立临时目录**，不会碰 `data/clips` 里你剪好的东西。

---

## 4. 模型选择（硬性）

### 4.1 文本：只用 DeepSeek

**文本内容只用 DeepSeek，MiniMax 只用于 TTS 与音色克隆。** 三层固化，不是靠注释：

| 层 | 机制 | 拦掉什么 |
|---|---|---|
| 启动 | `config.assert_text_provider()` 非 deepseek 直接抛错 | 有人改了 `config.yaml` |
| CLI | `--provider` 只接受 `deepseek`（argparse 直接退出 2） | `--provider minimax` |
| 运行时 | 每次运行打印分工横幅 | 「到底是不是在用 DeepSeek」的疑问 |

每次运行都会看到：

```
文本模型 (选题/写稿/史实审校) : deepseek / deepseek-chat
语音合成 (TTS)                : minimax / audiobook_male_1
```

要换文本模型：同时改 `src/hsg/config.py` 的 `TEXT_PROVIDER_LOCK` 和
`config.yaml` 的 `llm.provider`，两处必须一致。

### 4.2 语音：固定用系统音色 audiobook_male_1，语速 1.1

```yaml
tts:
  minimax:
    voice_id: "audiobook_male_1"   # 系统音色（默认）
    speed: 1.1
```

- 2026-09-14 曾换成克隆音色 `hsg_story_v2`，试听后认为听感不佳，当天改回系统音色。
  克隆流程本身可用，`hsg_story_v2` 仍挂在账号下：
  `data/base_voice/api-response_2.mp3`（15.87s 参考音频）经 MiniMax `voice_clone` 克隆而来。
- 克隆/换音频：`python scripts\clone_voice.py --check` 先验门槛，再
  `--voice-id <新id>`；工具会打印该往 config 里填什么。
- **换音色或改语速后必须重新校准 `story.chars_per_second`**，详见 §6。
  校准用 `python scripts\calib_rate.py`（0 成本，从已生成期的逐分镜时长反算），
  不要直接填 `probe-tts` 的范文值 —— 那个偏快。
- 想看两个音色的差别：`python scripts\tts_preview.py --script <脚本.md>
  --voice audiobook_male_1 --voice hsg_story_v2 --announce`。

---

## 5. 密钥（三路兜底）

`DEEPSEEK_API_KEY` / `MINIMAX_API_KEY` 本机在 **Windows 系统级环境变量**里，
正常不用配。但有些从图形界面启动的终端会把子进程环境清洗一遍（实测 Hermes 里
Python 读到的是空字符串），所以读取顺序是：

1. 进程环境变量
2. 项目 `.env`（参考 `.env.example`，会被 .gitignore 忽略）
3. **Windows 注册表**里的 Machine / User 环境变量（`src/hsg/config.py` 的
   `_registry_env`）—— 这是绕开清洗的那一层

三种途径任意一种有值就能跑，运行时会打印 `API Key：deepseek=已读取 minimax=已读取`。

---

## 6. 已知限制 / 需要注意

- **时长靠章节数而不是靠"目标时长"**。`story.target_total_seconds` 只用来算比例
  和打日志；真正决定内容量的是 `story.chapters` 和 `seconds_per_chapter`。
- **语速换算必须实测，而且必须跟上音色**。当前 `chars_per_second: 4.70` 是
  系统音色 `audiobook_male_1` + `speed=1.1` 的实测值（**只统计验证过音色的那 4 期**：
  72 段 / 8933 字 / 1900.0 秒）。
  ⚠️ 换算值要用 `python scripts\calib_rate.py` 从**已生成期的逐分镜时长**反算，
  不要直接填 `run.bat probe-tts` 的范文值 —— probe-tts 是连续合成，比流水线的
  分镜合成快一截（克隆音色那次：范文 3.91 / 实际 3.45；系统音色 4.77 / 实际 4.70）。
  ⚠️ **校准必须按音色分组，不同音色不能混着平均**：上一版这里的值是 4.59，多算了
  「白银、鸦片与钦差」那一期（4.14 字/秒）—— 那期用音频缓存名反查**任何候选都没命中**，
  根本不是这个音色录的。音色之间差得很远（克隆音色 3.45 vs 系统音色 4.70，差 36%），
  拿错值去估时长，成片长度和素材清单里的片段长度就全错了。
  `calib_rate.py` 现在会自动排除验证不过的期并标注。
  换音色或改语速后必须重算，否则初始估算偏、多花一轮改写 + TTS。
  参考：edge 的 `zh-CN-YunxiNeural` 实测 4.75 字/秒@1.0、5.22@1.1。
- **素材摘要质量一般**（Bing 摘要会混进游戏站、诗文站）。它的定位只是锚点，
  事实准确性最终靠审校层。
- **配图版权**：默认 `license_policy: clean`，只用 CC0/公共领域/免费商用图库
  （实测能用的只有克利夫兰艺术博物馆一家，见 3.5）。Bing（版权未明）只在
  `mixed` 策略下兜底。想扩充图源就去领 Unsplash/Pexels 免费 key 填环境变量。
- **TTS 兜底必须真的装上**。`tts.fallback_edge_tts: true` 只是代码路径 ——
  2026-09-11 真踩过：MiniMax 余额耗尽（错误码 1008），18 段语音全灭，
  而兜底报的是 `No module named 'edge_tts'`。已 `pip install edge-tts` 修好，
  现在 `run.bat --tts-provider edge` 可以用免费音色跑通全流程（试音/验证用）。
- **BGM 默认开着**（`bgm.enabled: true`），而且是 `bgm.playlist` 里**三首按章节交替**
  （第 i 章用第 i % 3 首），音量 0.08、开场白念完才淡入、循环铺满。
  两件事缺一听感就崩：① **响度归一化**（EBU R128 两遍法到 -18 LUFS）——
  候选曲录制电平差 21 dB，不归一化低电平那首在 0.08 音量下等于静音；
  ② **按「有声区间」切片** —— 有的曲子开头几十秒是无声铺垫，按 0 偏移切就是静音。
  换曲：把 mp3 放进 `data/bgm/playlist/` 并改 `bgm.playlist.files`；
  许可与实测指标记在 `data/bgm/来源与许可.md`。**不要用有版权的影视/节目配乐。**
- 渲染时间：一段 20 秒竖屏片段在 preset=medium 下约 10 秒编完，18 个分镜 ×2 个
  朝向 ≈ 6-8 分钟。想更快把 `video.preset` 改成 `faster`（实测差别不大）。

---

## 7. 生成记录与选题去重（不重复讲做过的故事）

每次跑完自动追加一条记录：

```
data/history.json     机器可读的完整记录（查重就靠它）
data/生成记录.md       人看的表格版（累计期数、时长、配图命中、校验情况）
```

看一下做过什么：

```bat
run.bat history                 :: 列出已生成的期目
run.bat history --backfill      :: 把 data/output 下已有的 metadata 补录进来
```

去重是两层的，因为「同一个故事换个说法」纯规则抓不住：

| 层 | 判据 | 成本 |
|---|---|---|
| 规则层 | 规范化后相同 / 一方是另一方子串（≥3 字）/ 二元组 Jaccard ≥ 0.55 | 免费 |
| 模型裁定层 | 只在「沾边但规则判不出」时调一次 DeepSeek，问「这两个题材是不是同一个历史事件」 | 一次便宜调用 |

实测效果：

  · `-t "鸿门宴"` → 规则层命中（子串），直接拦下，退出码 2
  · `-t "鸿门宴：项羽为什么放走了刘邦"` → 规则层判不出来（Jaccard 只有 0.19），
    模型裁定层判定「与已做过的《鸿门一宴，项羽为何放走刘邦？》是同一个故事」→ 拦下
  · 不带 `-t` 自动选题 → 自动跳过做过的题材（每次都会打印「已生成 N 期，本次已避开做过的」）
  · 选题池（小切口 30 条 / 事件式 32 条）全部做过之后 → 让 DeepSeek 出一个没做过的新题
    （出重了会重出，最多 3 次）

**选题是两级的**：L1 是**故事类型**（受控词表，每类一句高层描述说明这类故事讲什么），
L2 是**详细标题 + 与这个故事相关性最高的描述**。两级都会落进 metadata、
`_脚本.md` 头部与 `history.json`（老记录没有类型字段时按标题回查池子补）。
类型不是装饰，它让选题能**避开连着做同一路**（`pick()` 优先挑近期没做过的类型）、
能按类挑题（`--type`），也让大纲知道该往哪边展开。
看池子：`run.bat topics`（`--type "刑狱与流放"` 只看一类）。

被拦下时的提示：

```
ERROR  这个题材已经做过了：鸿门一宴，项羽为何放走刘邦？（2026-09-11，6.8 分钟）
         · 想换一个：去掉 -t/--topic，选题会自动跳过做过的题材
         · 确实要重做：加 --allow-duplicate
```

想去掉这个机制：`config.yaml` 里 `runtime.record_history: false`（记录仍可手动用
`history --backfill` 生成，但不再查重）。

记录里存了什么（`history.json` 一条的样子）：

```json
{
  "run_id": "20260911-114614",
  "generated_at": "2026-09-11T11:46:14",
  "topic": "虎门销烟：禁烟背后的账本",
  "title": "白银、鸦片与钦差：虎门销烟前的三本账",
  "chapters": 6, "scenes": 18, "chars": 1827,
  "speech_seconds": 442.0, "speech_minutes": 7.37,
  "video_seconds": {"portrait": 489.5, "landscape": 489.5},
  "images": {"total": 18, "found": 18, "fallback": 0},
  "verify": {"rule_fail": 0, "rule_warn": 0, "audit_issues": 3, "regenerated_chapters": [5]},
  "outputs": ["...\\_竖屏.mp4", "...\\_横屏.mp4"],
  "text_model": "deepseek/deepseek-chat",
  "tts": "minimax/audiobook_male_1"
}
```

---

## 8. 产物位置

```
data/output/20260911_<标题>_横屏.mp4        成片
data/output/20260911_<标题>_竖屏.mp4        成片
data/output/20260911_<标题>_脚本.md         口播稿 + 章节结构 + 配图出处
data/output/20260911_<标题>_metadata.json   全量元数据（分镜/时长/配图/时间轴/校验）
data/history.json                          生成记录（查重依据）
data/生成记录.md                            生成记录（人看的表格版）
data/material/                             素材缓存
data/audio/                                每个分镜的语音（文件名带文本哈希）
data/images/                               配图 + _sources.json
data/bgm/story_bed.mp3                     单曲 BGM（已启用）
data/bgm/playlist/*.mp3                     三首交替用的曲目（按章节轮换）
data/bgm/来源与许可.md                      曲名/作者/许可类型/实测指标（发布举证用）
data/slides/<朝向>/                         画面（*_bg.jpg 配图层 / *_fg.png 文字层）
data/segments/<朝向>/                       分镜片段 + _concat_*.mp4（拼接母版）

影视切片路线（路线 B）另外产出：
data/needs/20260915_<题材>_素材需求.md       剪素材的工作单（照着这份剪）
data/needs/20260915_<题材>_素材需求.json     同一份清单的机器版（出 EDL 时读）
data/clips/index.json                      素材库索引（每条素材的片名/年份/人物/绑了哪些槽位）
data/clips/norm/*.mp4                      规范化后的素材（统一画幅、已去音轨）
data/edl/20260915_<标题>_EDL.json           剪辑表（AI 排的镜头序列 = 第几秒用哪条素材的哪一段）
data/output/<日期>_<标题>_素材出处.md        发布举证：每条素材的片名/年份/占比/用在哪些分镜
```

---

## 9. 排查顺序（媒体出问题不要先怀疑流水线）

```bat
run.bat smoke          :: 零 LLM 跑通 配图→画面→字幕→编码→拼接（路线 A）
run.bat smoke-clips    :: 零 API 跑通 合成素材→绑槽位→排镜头→按镜头渲染→自检（路线 B）
python scripts\smoke_video.py --no-tts    :: 连 TTS 都不用，完全不花钱
```

`smoke` 通过说明媒体层没问题，问题在内容层（就先看 `plan` 的输出）；
`smoke` 不通过，报错信息能直接定位到是配图、Pillow 合成、ASS 还是 ffmpeg。
切片路线出问题优先跑 `smoke-clips` —— 它用 ffmpeg 合成的假素材、**不碰你剪好的素材库**。

切片路线卡住时先看状态（它会直接告诉你卡在哪一段、下一步敲什么）：

```bat
run.bat agent
```

判断版面（标题和字幕有没有打架）不要靠肉眼：

```bat
ffmpeg -hide_banner -loglevel error -y -ss 12 -i <成片> -frames:v 1 frame.png
python scripts\check_layout.py frame.png
```

---

## 10. 更新记录

> **约定（2026-09-15 起）**：git 提交信息**只写一句话**（说清这一轮干了什么就够），
> 详细改动、踩到的坑、验证数据全部记在**本节**。提交前先把细节追加到本节顶部。
> 为什么：commit message 是要在 `git log` 里一眼扫过去的，塞进十几行说明没人读；
> 而这些「为什么这么改」的信息对后面维护的人（包括 AI）很值钱，放这里才找得到。
>
> ⚠️ 内存紧的机器上 `run.bat test` 可能偶发 `x264 malloc failed`（`test_clip_normalize`
> 要编 1080p；实测本机 14GB 被吃到 92% 时会挂）。**重跑一次即可**，别去改断言 ——
> 判据是「隔离跑同一条也失败 + 代码路径当天没改」就是环境。要稳就先关掉几个 VS Code/浏览器。

### 2026-09-17 · 第 2 集《一道圣旨，能调得动几路兵马？》（曹操·挟天子）：出稿 → 逐条核史实 → 出片

开工就发现系列进度是坏的（见下条），修完才正确选中第 2 集。稿件由 MiniMax 写、DeepSeek 出大纲与审校，
**人工通读改了两轮共 32 处**，外加图注 10 条 —— 全部对着《三国志》（含裴松之注）原文核过，
65 卷全文副本留在 `data/tmp/sgz/`（`wd.txt` = 卷一 武帝纪）。

- **硬错清单（挑重点）**：
  ① 「曹操兵力不足万人」是把**官渡（200 年）**的记载挪到建安元年当家底用，而这条记载本身
     还被裴松之质疑过 → 改用初平三年「受降卒三十餘万…号为青州兵」；
  ② 「兖州刺史」→「兖州牧」（《荀彧传》「太祖领兖州牧」）；假节钺发生在**洛阳**（八月），
     不是到了许县才给；
  ③ 荀彧被写成「196 年冲着奉天子来投」—— 他初平二年（191）就已投曹，**早五年** → 换成郭嘉、荀攸；
  ④ 「急责租米」四字与所托的《张绣传》《后汉书·刘表传》**全无出处**（《三国志》全书查无此语）→
     换成《钟繇传》的实名链条：持节督关中诸军 → 马腾韩遂「各遣子入侍」→ 官渡送马二千馀匹；
  ⑤ 第 3 章原写「征粮诏书」，查无史料 → 改用建安元年「始兴屯田」「得谷百万斛」，
     把「诏书能换名分、换不来粮食」这条对照立起来（这是本片真正的论点）；
  ⑥ 袁绍要求迁都的是**鄄城**不是邺城；「袁绍大惧」无出处 → 删，换成《武帝纪》十月那场官职议价
     （绍耻班在公下不肯受太尉 → 曹操让大将军 → 自拜司空、行车骑将军）；
  ⑦ **「孙策不理诏书」与史实相反**：《江表传》载孙策建安元年、三年两次遣使贡方物（后者「倍於元年所献」），
     受制书拜讨逆将军、改封吴侯，还奉诏敕与曹操等并力讨袁术 → 第 4 章标题改为「袁绍嫌官小，孙策拿它当旗」；
  ⑧ 《让县自明本志令》是建安十五年（210）的，比情节晚十三年 → 删；
  ⑨ 赤壁「烧了三天三夜」无出处 → 按《武帝纪》＋裴注引《山阳公载记》改写（大疫、华容道负草填路、
     曹操自评「向使早放火，吾徒无类矣」）；连船的是曹操不是黄盖（《周瑜传》黄盖原话「观操军船舰首尾相接」）；
  ⑩ 三处假托出处/无据细节删掉：「《三国志》记录了时人的评价」「士燮换个年号交了差」「曹操征召刘表之子被拒」。
- **未采纳复检 2 条并写明依据**：「领司隶校尉」依裴注《献帝纪》系于洛阳；讨袁术名单里的刘璋
  是《江表传》原文。原则：模型对原典的二次判断与原文冲突时，**以原文为准**。
- **图注漏改（本轮教训，已升为护栏 48）**：正文改了 32 处，`caption` 一条没动，第一次渲染出来的画面里
  还挂着早被删掉的情节（旁白与画面互相打脸）。补改 10 条图注后重渲。
- **成品**（两版同规格）：`data\output60917_一道圣旨，能调得动几路兵马？_横屏.mp4` /
  `_竖屏.mp4` —— 573.9s / 1920x1080@30 与 1080x1920@30 / aac 48k；封面均色 R-B＝100（横）与 109（竖）；
  字幕墨迹 34%–66%（横）、17%–83%（竖），未溢出；**切片占比 0.0%**、素材出处清单「使用影视素材 0 条」；
  配音 18/18 段 512.1s（8.54 分钟）；配图 18/18 张全部生成（image-01）。
- 验证：回归 598 项全过；`run.bat series` 进度 **2/10**、下一集第 3 集《司马懿》。

### 2026-09-17 · 修「读到 story 再写出去会洗掉待人工核对清单」（第 2 集写稿时踩到）

做第 2 集时发现：改完稿重新生成脚本文档，`## ⚠️ 待人工核对` 那一节变**空**了。

- **根因**：字段名不对称。`pipeline.write_metadata` 把 `story.notes` 写成 metadata 的
  **`unresolved`**，而 `storyio.load_story` 只读 **`notes`**。于是「load_story → 改 →
  再 write_metadata」这一步会把整份清单洗掉。影响不止文档：阶段 2 出新版 metadata 时
  走的正是这条路径（`_prepare_story` → `_record_finished`），**成片 metadata 里的
  待人工核对清单也会跟着空** —— 发布前最该看的那份反而没了。
- **改动**：`storyio.load_story` 读 `notes` 时回退到 `unresolved`；新增回归测试
  `test_script_notes_roundtrip`（落盘字段名 → 读回 notes → 再写不丢，往返稳定）。
- **验证**：脚本 md 重新生成后待人工核对清单 **38 条**回来了；回归 **598 项全过**。

### 2026-09-17 · 修「阶段 2 出片不写生成记录」：系列进度从 0/10 变回 1/10

开工做第 2 集前的 30 秒自查（`run.bat series`）暴露出一个真问题：第 1 集明明出过两版成片，
进度却显示 **0/10**，「下一集」还是第 1 集 —— 照这样跑，系列会一直在第 1 集原地打转。

- **症状**：`data/history.json` 自 2026-09-14 起再没长过（只有 7 条，全是最早整跑留下的）。
- **根因**：出片有两条路 —— `run`（`pipeline.run_pipeline`，写完记录再收工）和
  `agent --stage 2`（`agent.stage2`：渲染完只出剪辑表 / 自检 / 素材出处，**既不写生成记录、
  也不写成片 metadata**）。自 09-15 转入「阶段闸门」流程后，**所有**成片都从阶段 2 出，
  记录于是断了。而护栏 25 要求系列露在三处，其中一处正是**生成记录**：
  系列进度（`topics.series_progress`）与「下一集是哪集」（`topics.next_episode` → `history.is_used`）
  全靠它算，所以断了记录 = 系列进度与选题推进一起失灵。
- **改动**：新增 `agent._record_finished()`，阶段 2 渲染完落两样东西 ——
  ① 成片 metadata（`data/output/<日期>_<标题>_metadata.json`，带 outputs / covers /
  video_seconds / **tts_spec**（音色语速，rerender 旧期要靠它）/ plan_metadata，`plan_only: false`）；
  ② 生成记录。另新增 `history.upsert_record()`：**按标题更新**而不是无脑追加 ——
  阶段 2 会反复重跑（换开场白重出、补一版竖屏），纯 append 会把同一期堆成多份副本，
  并且会保留 `first_generated_at`（重渲染不该把「这期几号做的」往后推）。
- **backfill 加例外**：plan 产物默认不算一期（它只是中间稿），但**成片确实躺在 output 里**
  （>1KB）的例外 —— 用来认回 09-15～09-17 之间出过的片。实测补录 2 条：
  第 1 集的两版（《霍光废帝》与《换皇帝这份奏折》）。
- **验证**：`run.bat series "古代十大权臣"` 现在报 **进度 1/10**、第 1 集「✓ 已出（2026-09-17）」、
  下一集第 2 集《曹操：挟天子到底能令多少诸侯？》；回归 **595 项全过**
  （新增 17 条：成片 metadata 落盘/系列字段/tts_spec/记了成片路径/记录带系列/
  upsert 不堆副本/保留 first_generated_at/系列进度认到 1/10/下一集往前走/backfill 有片才认）。
- 已知小瑕疵：补录记录的「配图」列是 0/18 —— plan 产物里 `image` 字段都是 null
  （配图发生在它之后的阶段 2），真实配图在 `data/images/scene_*.jpg`。只影响记录表里这一列。
- 踩坑：本轮改 `history.py` 时手滑删掉了 `save()` 里的 `p = history_path(cfg)`，
  被同一次编辑的 diff 立刻暴露并改回 —— 说明「改完就看 diff」比「改完就提交」重要。

### 2026-09-17 · 改用 MiniMax 写稿（DeepSeek 校验）+ 画面全用生成图 + 重出这一期
（用户要求：脚本改用 MiniMax 生成、注意语义连贯与史实准确、讲述由浅及深有层次；
 完成后用 DeepSeek 做史实与语法校验；删掉旧语音重新生成；删掉网上找图，背景全部用分镜生成图。）

- **写稿模型切换**：`llm.provider: minimax` + `llm.allow_nondeepseek_text: true`；
  新增 `verify.provider: deepseek`（默认）—— 写稿与校验**故意用两个模型**（同模型常看不见自己的错）。
  原来的硬锁 `TEXT_PROVIDER_LOCK` 改成「默认 deepseek + 显式开关 + 横幅三行打印」，
  不是把闸门删掉：不写 `allow_nondeepseek_text` 就启动即报错。
- **由浅及深写进提示词**：大纲层的结构要求改成一条**不许跳级的梯子**
  （第 1 章只讲"发生了什么"不下结论 → 中间讲"靠什么做到／制度上为什么能／代价落在谁身上"
  → 末章才给判断），并要求**问题链**（每章开头接住上一章留下的问题、结尾留下下一章要答的问题）；
  写稿层加了两条：每章第一句要承接、章内先场景后机制再落回人的处境、同一概念前后同一个词。
  实测效果：大纲出来就是「27 天出了什么问题 → 权力是武帝递过去的 → 13 年把对手一个个熬走
  → 几十个签名怎么来的 → 代价从什么时候开始算 → 那张奏折上的最后一个签名」。
- **画面全用生成图**：`images.providers: []` + 新增 `generate_only: true`（失败重试 3 次，
  全败才退回渐变底图）；同时关掉漫画与切片 → 成片画面 100% 由分镜生成图构成。
- **旧语音已删并重出**：删 153 个 mp3（59.9MB，清单在 data/tmp/deleted_audio_20260917.txt），
  重渲时 18 段全部重新合成（440.7s）。⚠️ 清理用的是 `scene_*.mp3` 通配，把其他期同名分镜的
  语音缓存也顺带删了 —— 那只是缓存，重渲会重新合成，不影响产物。
- **新稿复核（这是本轮最有价值的部分）**：MiniMax 的稿**结构更好、史实更毛糙**。人工通读 +
  DeepSeek 复检共改 22 处，其中史实硬错 8 处：编造的「上面只有四个字——行邪伐宗」（汉书无此语）、
  武帝病倒写前 89 年（应为前 87）、丞相写成车千秋（前 74 年丞相是杨敞）、
  田延年那句狠话按《汉书》原文改回「今日之议，不得旋踵；群臣后应者，臣请剑斩之」、
  「翁婿」改「儿女亲家」、「率兵进围昌邑国」改为朝议请奏＋准奏后收捕（DeepSeek 抓的）、
  「死后三年」改两年、「诸霍故人」改指霍家党羽；签名人数由「二十二」改为「几十个」
  （汉书联名名单我数过约三十余人，数字存疑就不点死数）。
  另外 DeepSeek 也抓到了**我上一版改稿引入的时间线矛盾**（分镜 10 说"太后点头"、分镜 12 又"去见太后"）
  —— 交叉校验的价值就在这儿。
- 验证：回归 576 项全过（新增 5 条：provider 白名单/显式确认/校验固定 deepseek/LLM 覆盖/只用生成图不回退图库）；
  顺带修两条把 deepseek 写死的老测试（改成跟配置比，否则一改配置就挂且看不出原因）。

### 2026-09-17 · 霍光那期脚本复核：捞出 6 处史实/表述问题并改稿（附审校器噪声治理）
（用户要求「重新验一遍脚本，核查史实和语句是否通顺」。）

- **先看当初的闸门记录**：metadata 的 `unresolved` 里躺着 12 条——`block_on_fail: false` 的后果，
  有问题也照常出片了。逐条人工分拣后：**6 条是真问题、6 条是审计器自己判了「此条不报」的噪声**。
- **真问题与改法**（改的是 metadata，脚本 md 是它的渲染视图）：
  1. 「死后三年」错成两年，共 4 处（第 6 章标题、章要点、分镜 17、分镜 18）——
     前 68 年霍光死、前 66 年霍家灭。**班固引文里的「死财三年」保留**（含头尾的算法，引文不动）。
  2. 分镜 6「一个被灭族，一个被灭族」——纯重复，且「剩下的」其实只剩霍光（金日磾前 86 年已故）。
  3. 分镜 10 引用了大纲标【存疑】的锚点（「史书上说大将军节制卫尉」）——违反项目自己的规矩，
     改成「史书没有明说…但那时候这些关键职位上都是他的人」。
  4. 分镜 9「干了一千一百二十七件错事」把罪状写成了事实 → 「被罗列了一千一百二十七条罪状」。
  5. 开篇「没有兵符，没有虎符」——同义重复，且与第 4 章「宫门在他手里」自相矛盾 → 改成
     「没有调得动边军的虎符，可长安城最关键的那几道宫门，钥匙在他手里」。
  6. 分镜 1「四个人，霍光排第一」→ 回避「四人还是五人」之争（遗诏受命者四人，但流行的说法
     常把丞相田千秋也算进来），改成「另外几个人…这几个人里，霍光排第一」。
  另按审校建议补了上官安（政变核心人物）、分镜 2 去句子重复、分镜 12 说清「增封＋原食邑＝二万户」。
  改完字数 2149 → 2209（+60 字，约 +13 秒），语音总长仍在 300-600 秒区间内。
- **审校器噪声治理（代码）**：新增 `verify.triage()`，把「此条不报 / 此条无误 / 无需修改」这类
  **它自己都判了没问题**的条目从「待人工核对」清单里过滤掉（三种说法是三轮实测出来的，还会再变，
  所以宁宽勿窄）。改之前：某期 12 条里 6 条是噪声、另一轮 21 条里 18 条是。
- **顺手修的**：`cli.py` 里新加的 `verify.triage` 少了个 import，被自己加的 **pyflakes 门槛**当场拦下。
- 验证：回归 559 → **564 项全过**（+5：triage 三种标记、只留真报项、空输入不炸）。

### 2026-09-17 · 镜头全部改用生成图（关掉剪辑片段）+ 封面换明亮橙黄
（用户要求：所有镜头用 AI 生成的四格漫画 + 历史图像，不再用剪的影视片段；封面明亮一些的橙黄色。）

- 新开关 **`clips.enabled: false`**（默认已关）：`build_edl` 里**连"挑素材"这一步都不做**
  （`direct_edl` 与 `scene_candidates` 一起跳过）。为什么不只是"渲染时不用"：导演会按绑定的
  切片排镜头，等于白问一轮、还可能把切片带进成片。实测干跑：66 个镜头 = comic 6 / still 59 /
  generate 1，**切片占比 0.0%**。素材库没删，改回 true 就能恢复混排。
- **四格漫画按"出了图才用"**（`_fallback_shots` 里查 `comic.sheet_for`）：现在 6 个章节开场
  的槽位（s01/s04/s07/s10/s13/s16_sh1）是四格漫画、其余 60 个槽位是生成的配图。
  不必为了统一把 66 个槽位都出漫画（那要 30-60 分钟 + 相应额度）。
- **封面换明亮橙黄**：`cover_tint` → `#F0A22A`、`cover_tint_alpha` → 0.78、
  `cover_base` → `#C07020`、`cover_darken` → 0.10。实测（真实配图当底，偏暗）：
  R122 G87 B34、R-B=89、亮度 91 —— 判据是「R>G>B 且 R-B>60 且亮度≥85 且 R-G≥20（偏黄不偏红）」，
  四条都写进回归了。参数是扫了 4 档量化挑出来的，不是拍脑袋。
- **踩到"测试依赖全局配置"的坑（好事）**：把 `clips.enabled` 默认改 false 之后，
  回归里 3 条用例、冒烟里 1 条用例当场挂掉 —— 它们测的就是切片链路，却靠全局默认值成立。
  已全部改成**显式打开**（`cfg.clips["enabled"] = True`），并且用临时 `comic.dir` 隔离
  （磁盘上真有 data/comics/s01_sh1.jpg，不隔离会让"回退画面"用例测到别的期去）。
- 验证：回归 551 → **559 项全过**；`run.bat smoke-clips --small` **34 项全过**。

### 2026-09-17 · 真 bug 二号：EDL 渲染路径自己拼 kicker，系列名压根没上过屏幕（+ 配图复用开关）
- 现象：修完 storyio 重渲一版后，抽帧验收发现画面顶部小字还是
  「历史小故事 · 第1章 遗诏辅政：权从哪来？」——**系列名与期号没有出现**。
- 根因：`pipeline._render_scene_shots`（EDL 渲染路径）里是自己拼的
  `f"{channel} · 第{ch.index}章 {ch.heading}"`，**根本没走 `kicker_text()`**；
  而 `kicker_text` 才是带系列名的那份口径（另一条渲染路径和封面都在用它）。
  同一个 f-string 还顺手把章节标题又写了一遍（下面那行大字就是它，视觉复核早先就提过"顶部小字和中部大标题重复"）。
- 为什么测试没抓到（又一次同一类坑）：测试钉的是 `kicker_text` 本身，
  而真实渲染路径压根不调它。修法：渲染路径改走 `kicker_text`，
  并加一条**代码形状守卫**：`pipeline.py` 里再出现 `kicker = f"...第{` 这种自己拼的写法就 FAIL
  （跟 pyflakes 门槛一个思路：防的不是这一次，是下次又分叉）。
- 顺带加 **配图复用**（`images.reuse_existing: true`）：重渲时若 `scene_XXX.jpg` 已在、
  且**提示词跟上次一模一样**（从 `_sources.json` 的 attempts 里反查），就直接复用、不调 image-01。
  背景：重渲一次就把 18 张图重烧一遍（约 10 分钟 + 额度），而重渲通常只是想换个开场白/布局。
  提示词变了自动重出，`--force` 强制重出。实测：这一次重渲 18 张全部复用，渲染时间明显短一截。
- 附带：写这个复用时漏了 `import json`，被自己加的 **pyflakes 门槛**当场拦下（真跑才崩的那类错误）。
- 验证：回归 546 → **551 项全过**（+5：反查到上次提示词、一致→复用、不一致→不复用、
  开关关掉→不复用、渲染路径 kicker 形状守卫）。

### 2026-09-17 · 真 bug：stage 2 重建 story 时丢了系列字段（开场白与系列标签在成片里都没生效）
- 现象：上一轮改完开场白后重渲，成片里**开场白还是旧的**（片头语音 42.8s，新开场白
  221 字应当是 ~47s）。查音频缓存：新文本的 tag `e5ce3f69` **根本没生成过**。
- 根因：`storyio.load_story`（stage 2 从 metadata 重建 story 的那条路）**没传
  `series` / `series_ep`** → `story.series` 为空 → 开场白退回旧句式，画面顶部的
  系列标签也跟着一起丢。这是系列那轮（`055a7a1`）留下的漏网之鱼。
- 为什么冒烟没抓到：冒烟**直接构造 Story 对象**，从不经过 storyio 这条真实路径 ——
  正是护栏 38「阶段 2 必须在真跑之后才算验过」说的那类坑。
- 修法：`load_story` 带上 `series`/`series_ep`，并补 4 条**走真实路径**的测试
  （写 metadata → `load_story` → 开场白与画面标签里必须都有系列名与期号）。
- 附带（封面橙色调足）：0.46 → 0.54 → **0.62**。实测每次都是「合成图测得出、真实封面差一口气」：
  0.46 时真实封面 R-B 只有 36、0.54 时 39，降到 0.62 才到 **42**（判据线 40）。
  测试里也补了一条**冷色配图当底**的算例（渐变图测不出这个差距），基线 30（冷底实测 35、真实期 42）。
- 复现/验收方式：新开场白 221 字 → 音频 `data/audio/intro_e5ce3f69.mp3` **46.1s**（旧的 42.8s/199 字），
  已先合成好放在缓存里，下次 stage 2 直接复用，不再多花钱；
  重渲命令：`run.bat agent --stage 2 -o landscape --metadata "data/output/20260916_plan_..._metadata.json"`

### 2026-09-17 · 四格漫画素材路线（image-01，出图前先扩写四拍）
（用户点子：每个 7 秒镜头的画面不再手工剪影视切片，改成**按场景生成四格漫画**。）

- 新增 `src/hsg/comic.py` + `run.bat comic`：槽位（s01_sh1…，与素材需求清单**同一套编号**）
  → ① LLM 扩写四拍 → ② 拼提示词 → ③ MiniMax `image-01` 出图 → ④ 写「漫画提示词清单」
  （`data/comics/<期>_漫画提示词.md`，人能读、能改）+ 槽位映射 json。
- **扩写要求写在提示词里**（这是用户明确要的一步）：四格必须是**同一场戏的四拍**
  （起/承/转/合：交代人地时 → 推进 → 结果落点），每格 25-45 字只写**看得见的东西**
  （动作、器物、光线），并给一句「人物与场景统一设定」在四格里复述 —— 跨格一致全靠它。
  实测扩写质量可用（霍光那期 6/6 全部走 LLM 四拍，设定里写清了「四十岁、清瘦、短须、
  进贤冠、绛色朝服」）。LLM 不可用时退回**机械四拍**（按标点切句），流程不断。
- **四格在一张图里**（不是出四张）：四格的灵魂是格与格之间人物一致；但实测一张图也
  没做到 100%（冠帽有变、某格少个人）—— 这属于模型能力上限，如实记录，别当已解决。
- 渲染接上：`comic.enabled: true` 时 EDL 的兜底镜头改成 `kind=comic`，
  `shotvideo.encode_comic_shot` 把 2x2 **一格格轮流上屏**（每格 shot/4 秒，缓移+叠字
  走原来的静态图链路，章节标题只在第 1 格）。
- **修掉一个会切头切脚的问题**：漫画格是方形、画布是 16:9/9:16，原来只有 cover 铺图
  （方形进宽画布上下各裁 44%，实测把人物头部和卷轴下端切掉了）。新增
  `media._blurpad` + `build_layers(..., fit=)`，漫画走 `comic.fit: blurpad`。
  验收：格子上沿画绿带、下沿画品红带 —— blurpad 两条都在（0.00-0.12 / 0.88-1.00），
  cover 两条全没了（判据有分辨力，也证明 16:9 下确实会切）。
- 验证：`run.bat comic --must-only` 真出 6 张（并发 4 路，约 1.5 分钟，25-50 秒/张）；
  回归 522 → **540 项全过**（+18：提示词/机械四拍/切格读序/一格格上屏的四色卡验收/
  blurpad 与 cover 对照）；一条 7 秒漫画镜头真渲出来是 7.07s，四个时间点各对应一格。

### 2026-09-16 · 关键词挪到左上角 + 开场白报系列 / 期号 + 封面橙色风格
（用户三项指定：① 关键词别压在画面正中，放左上角；② 开场白要说清 L1 系列名和 L2 片名；
③ 封面改成橙色风格。）

- **关键词（大字标注）改左上角**（`media.draw_callout` + 新常量 `CALLOUT_CX/CY`）：
  它原来压在画面正中（`CALLOUT_CY=0.46`）——而素材画面里最值钱的信息（人脸、朝堂全景、
  动作）恰好都在中间，等于每打一次标注就挡一次主体。
  · 章节标题**下移让位**（`TITLE_TOP` 0.085 → 0.20），两者同在上三分之一、互不重叠
    （像素验收：关键词带 0.075-0.19 有 6407 像素、旧正中位 0.40-0.52 **0 像素**、
     右上角 0 像素 ——「靠左」不是靠眼睛看的）。
  · 位置全部进配置（`callout_align/cx/cy`、`callout_max_width`、`title_top/bottom`），
    想回旧的居中样式只要 `callout_align: "center"`，不用改代码。
- **开场白报出系列与期号**（`pipeline.intro_speech`）：原来是
  `{栏目}。本期为您讲述《{片名}》。{钩子}` —— 观众点进来不知道这是系列的第几集，
  后面「上一位 / 下一位」的承接全是悬空的。现在系列集改成
  「{栏目}。从这一期开始，我们讲一个系列——《古代十大权臣》。这是第一期，霍光废帝：…」
  再自然接上开篇钩子；**非系列的单集句式不变**（不为统一硬塞「系列」二字）。
  期号念中文（`num_cn`：第 12 期 → 第十二期，避免口播「第一二期」）；
  另修一个标点 bug：片名本身以问号结尾时，机械补句号会拼出「？。」（`join_sentences`）。
- **封面橙色风格**（`media.build_cover` + `hex_rgb`）：底图不管原来是冷色还是彩色，
  一律按 `cover_tint_alpha`（0.46）往橙 `#C2571A` 拉一遍，压暗与遮罩也换成暖色
  （`cover_scrim`，纯黑会把橙色拉回冷调），栏目名下面加一条橙色装饰条当视觉锚点。
  · 按像素验收：封面均色 R63 > G35 > B19（R-B = 44）；把 `cover_tint_alpha` 归零 +
    冷色底再出一张则 R ≤ B（证明这个开关真的起作用，不是摆设）。
- **顺带修掉一个「假通过」的判据**（这轮最有价值的发现）：冒烟里
  「竖屏中间清晰带 vs 上下模糊衬底」的能量比判据**长期靠运气通过**——
  它在成片里抽 `-ss 6` 一帧来量，而那一秒放的是渐变占位图（整幅平坦，两边都接近 0，
  比了个寂寞）；抽到 `freeze_zoom`（定格放大，整幅铺满、根本不走模糊衬底）时更离谱。
  关键词一挪位置判据就失败，连查三轮（先怀疑几何、再怀疑素材、最后量逐行能量谱）
  才发现是**取帧时机**的问题，不是渲染的问题。改成：用**生产用的 `_fit_chain`**
  + 自造 12px 棋盘格素材直接出图验收，逐行能量取**中位数**（文字只占少数行，
  均值会被那一行字带偏），并加一条 `cover`（裁满）对照组证明判据能分辨两种 fit。
  判据由「1.1~5.8× 的糊涂账」变成 **37.8 vs 0.05（726×）**；
  合成素材也从 `testsrc2` 换成棋盘格（大色块/细噪声都不行：前者模糊后边界仍在，
  后者被编码磨平）。
- 验证：回归 505 → **520 项全过**；`run.bat smoke-clips --small` **34 项全过**（+3：blurpad
  生产链验收、cover 对照组）；`audit_config` 死配置 0 项。

### 2026-09-16 · 阶段 2 第 4 个真 bug：导演超长镜头会毙掉整集
- 现象：真跑整集时，剪辑表校验直接报 8 条错并**拒绝出片**：
  `s01_sh2：要从 hg_tuogu 的第 0.0s 起取 9.0s，但它只有 7.0s`。
  即 AI 导演让**一条 7 秒素材出 9~10 秒**（提示词里明明写了「src_in + dur 不能超过素材时长，
  会给你每条素材的长度」，它不听）。
- 问题不在"拦下"（拦得对），而在**拦下之后整集都不出了** —— 其实只有那几个分镜需要退让。
  修法：在 `direct_edl` 里逐镜头校验 `src_in + dur ≤ 素材时长`，命中就**该分镜**退回规则排法
  （跟"总长对不上"的处理一致），不再等到 `validate_edl` 把整集毙掉。
  实测重跑：6 个分镜各自告警退回，整集正常出片。
- 附带记两个环境事实：① 源片是 2240x1400（2.16:1），铺到 1920x1080 时 `increase+crop`
  会自动裁两侧，所以右侧水印本就会被裁掉一部分，但只差几十像素就会留竖条 → 还是显式裁；
  ② `video.preset` 默认 medium 在这台机器上**慢到不可用**（单个 1080p 片段 7 分钟，
  内存只剩 0.6GB 时疯狂换页），本次用临时配置 `video.preset: veryfast` 跑通，
  生产默认值不动（改不改由你定，见下面"待定"）。
- 验证：回归 500 → **505 项全过**（+5：超长镜头退回规则排法、不再有取超素材长度的镜头、
  总长仍铺满、校验通过）；冒烟 31/31。
- **真出一集的结果**（《古代十大权臣》第 1 集·霍光，6 条素材全部来自用户录的第 58 集）：
  成片 **527.8 秒（8.8 分钟）/ 1920x1080@30 / 音轨 aac**，18 场 70 镜头，**切片占比 8.8%**
  （红线 45%），封面 + 素材出处清单齐全。顺带改了个举证清单的列名：
  「时长占比」→「**使用时长**」（那列填的是秒数 20.7s，不是百分比，标题写错了）。

### 2026-09-16 · 第一次真跑阶段 2：查出 3 个真 bug（冒烟测不出来的那种）
- 背景：用户录了《汉武大帝》第 58 集（大结局）的托孤戏（72.8s 屏幕录像），要真出一集。
- **bug 1（会直接崩）**：`agent._prepare_story` 里写了 `tts_mod.TTS(...)` 但从没 import
  —— 冒烟测试用桩 TTS，永远走不到那行，**第一次真跑阶段 2 就 `NameError`**。
  修法不只是补 import：加了**静态门槛**（`test_pyflakes_gate`，pyflakes 拦
  `undefined name` / `redefinition`），先自检门槛有效性（造了个同样的 bug，确认能抓到
  `undefined name 'tts_mod'`），再把现有 32 条无害提示（未用 import 等）排除在外。
- **bug 2（裁错地方）**：一开始按"台标在黑边里"的假设裁上下 5%/6%，结果台标、右侧竖排
  「汉武大帝」水印**都在画面区里面**。改成**按像素量**：逐行亮度剖面定出结构
  （y0-107 纯黑页面头部 / y108-1143 画面 / y1168-1243 字幕带 / y1244-1400 纯黑），
  分格近白像素计数定出台标（左上 x240-640、右上 x1680-1840，都在 y108-227），
  偏黄像素计数定出右侧竖排水印（x1920-2079、y800-1130）。最后用
  `--crop-top 0.17 --crop-bottom 0.185 --crop-right 0.03` 裁干净。
- **bug 3（裁切只支持上下）**：影视源的角标可能在任何一条边，`normalize_clip` 只有
  `crop_top/bottom`。补上 `crop_left/right`（同一套 `trunc(.../2)*2` 取偶数逻辑，
  四边各自 <0.6 的护栏），`import_clip.py` 暴露 `--crop-left/--crop-right`。
- 另一个值得记的：源是 2240x1400（2.16:1），目标 1920x1080（1.778:1）——
  `increase + crop` 铺满时会**自动裁掉两侧**，所以右侧水印其实被宽高比裁切顺手去掉了一部分；
  但只差几十像素就会留一条竖条，所以还是显式裁右更稳。
- 素材：从这一条录像里切了 6 段（每段 7 秒，低于 10 秒红线），绑到 6 个必剪槽位，
  覆盖面 22 个槽位（剩下的由复用/生成图填）。这一集 6 条素材**全部来自同一集录像**。
- 顺带记：验收时我写过一个"近白/偏黄像素计数"的自动判据，结果把**画面本身偏亮/偏黄**
  误报成残留（4 条干净的被判为脏）。**像素判据只能验证几何（裁切区是否覆盖了量出的坐标），
  不能验证内容**；最终靠"肉眼 + 坐标几何"双重确认。
- 验证：回归 498 → **500 项全过**（+2：pyflakes 门槛）。

### 2026-09-16 · 片源下载（yt-dlp）实测结论：两个站都要 cookie
- 起因：问「B站和 YouTube 能不能下载成 mp4」。能，工具是 yt-dlp，已装进项目 venv
  （`2026.08.19`）。
- **在这台机器上实测的结论**（不是查文档抄的）：
  · B站不带 cookie → `HTTP Error 412: Precondition Failed`（风控）；补浏览器 UA + Referer 也没用；
  · YouTube → `Sign in to confirm you're not a bot`（网络是通的，请求已打到 player API；
    注意 curl 直接访问 youtube.com 会超时，但 yt-dlp 能到 —— 本机是 TUN/系统代理
    `127.0.0.1:7993`，需要时给 yt-dlp 加 `--proxy http://127.0.0.1:7993`）；
  · `--cookies-from-browser` 两条路都断：edge 报 `Failed to decrypt with DPAPI`
    （新版 App-Bound Encryption，yt-dlp issue #10927），chrome 报
    `Could not copy Chrome cookie database`（本机没有可读的 Chrome profile）。
- 所以可行路线写进了 `docs/生产线操作手册.md` §3.5：
  **A** 浏览器扩展导出 cookies.txt（`--cookies`）—— 推荐；
  **B** B站官方客户端下载后 `ffmpeg -i video.m4s -i audio.m4s -c copy out.mp4`（拷流不重编码）；
  **C** 录屏兜底（7 秒素材够用，注意别录进 UI/弹幕）。
  另记了 `--download-sections "*32:15-32:22"` —— **只下需要的那几秒**，省盘省时间；
  一集里要抠好几段时（《汉武大帝》末段要出 3 条）才整集下更划算。
- cookie 文件等于账号凭据：`.gitignore` 加了 `*cookies*.txt` 和 `data/downloads/`，
  绝不提交。
- 选片源优先**正片**，别用解说二创（画面里带别人的字幕/logo/水印，既难看又说不清出处）。

### 2026-09-16 · 剪素材不用剪辑软件了（按时间码直接抠）+ 硬字幕裁切
- 起因：问「推荐剪辑软件」。核实之后发现**这一步本来不需要剪辑软件** ——
  `scripts/import_clip.py` 早就有 `--in / --out`，可以直接指着一整集抠出 32:15–32:22
  这一段，顺手把该做的都做了（去音轨、转 1920x1080@30、绑槽位）。
  缺的只是两件跟"举证"和"画面干净"有关的事，这轮补上。
- **补 1：裁掉源片自带的硬字幕/台标**（`clips.normalize_clip` 新增 `crop_top`/`crop_bottom`，
  `import_clip.py` 暴露成 `--crop-top/--crop-bottom`）。为什么必须是"裁"：字幕和台标
  烧死在画面里，ffmpeg 去不掉。裁剪放在缩放**之前**，之后照旧 `increase + crop` 铺满画幅，
  所以**不出黑边**，代价是左右各损失一点 + 轻微放大。高度用 `trunc(.../2)*2` 取偶数
  （yuv420p 不吃奇数高）；上下相加 ≥0.6 直接拦下（画幅要变形了）。
  **按像素验收**：造假素材「上 80% 绿 + 下 20% 品红」，不裁时底部采样是品红(254,0,253)、
  裁 20% 后变成绿色，输出仍是 1920x1080、时长不变。
- **补 2：取源片段的时间码入档**（`--in/--out` → `src_in_sec`/`src_out_sec`），
  并出现在「素材出处清单」新增的「**取源片段**」列（如 `32:15–32:22`）与「裁切」列
  （如 `裁下 14%`）。「合理引用」最说不清的就是"用了哪一段"，这两列就是答案。
- 顺带记一个坑：`run_ffmpeg` 的 cwd 是**目标目录**，用相对路径传源文件会
  「Error opening input: No such file or directory」。探针脚本里必须 `.resolve()` 成绝对路径
  （`import_clip.py` 本来就 resolve 了，所以 CLI 路径没问题）。
- 验证：回归 489 → **498 项全过**（+9：裁切像素验收、对照组、画幅/时长不变、
  裁切与时间码入档、超量裁切被拦下）。

### 2026-09-16 · 取景单：剔掉「拍不到这场戏的剧」+ 多轮累积候选
- 起因：逐条复核霍光这期 6 处必须剪的镜头时，发现一个**系统性错误**：模型把
  「汉武帝驾崩之后十几年」才发生的三件事（废昌邑王、迎立宣帝、霍光之死）
  全都推荐给《汉武大帝》第 56–58 集，理由是「应在剧末」「结尾可能涉及」——
  而这部剧的主线只拍到汉武帝驾崩、霍光受遗诏辅政。剪辑师照这个去翻，白翻。
- 修法：候选新增 `cover_to`（该剧主线覆盖到哪件事为止）与 `in_cover`（in/out/unknown），
  **`out` 的直接剔掉**并记进 `dropped_out_of_cover`（不静默消失，日志会打出来）。
  再跑：模型自己就把《汉武大帝》《大汉天子》在那些槽位标成了范围外，并剔掉。
  表里也注明「已剔掉拍不到这场戏的候选，留下的仍以你能搜到为准」。
- 顺带修掉取景单的**抽奖性质**：模型每轮给的候选都不一样（同一处镜头这轮给《云中歌》、
  下轮就不给），覆盖式刷新等于每次重新抽。现在 `--stage sources` 会**把上一轮的候选并进来**
  （`merge_candidates`：按剧名去重、保留置信度更高的、补齐字段），跑几次越攒越全。
  实测连跑 3 轮，s01 攒到 4 条候选（含此前几轮给过的《汉武帝》），s04/s13/s16 也补齐了。
  自检怀疑的候选排到最后，不挡在前面。
- 小 bug：丢弃日志写在循环里，每处理一个镜头就把累计列表重打一遍（1 条、2 条、3 条…），
  移到循环外只打一次。
- 验证：回归 473 → **489 项全过**（+16：范围外剔除与记录、unknown 保留并标注、
  合并去重/置信度优先/补字段、多轮合并的标记、自检怀疑排最后、渲染「剧中覆盖到」）。

### 2026-09-16 · 取景单加「推算集数位置」（给参考，不给假精确）
- 需求：「精确到第几集的大概进度位置，不一定需要完全准确，给个参考」。
  这个要求本身是对的 —— 参考值能把「58 集里翻一场戏」缩到两三集，即使偏 1–2 集也值。
  关键是**别把参考包装成查证结果**。
- 每部候选片源现在给四样：`ep_range`（**推算**集数区间，如 56–58）、`episodes_total`（总集数）、
  `in_episode`（集内**分段词**：前段/中段/集尾）、`basis`（推算依据）。
  另加**全剧进度 %**（如 97%–100%）—— 这个百分比对「偏了几集」不敏感，比集数更耐用。
- 三条分工/底线（已写进 AGENTS 护栏 33）：
  · **分钟级时间码仍然一律剪掉**（`FABRICATED_TIME`）—— 那个精度模型给不准，印出来就是编的；
  · **总集数由模型给，进度 % 由代码算** —— 算数是确定性活，模型算不准也不该让它算；
  · **区间与总集数矛盾时不硬收窄**：上界超了就收到总集数并标出；整个区间都在总集数之外
    就原样保留 + 告警「它可能连总集数都记错了」（硬收窄等于替错的数字背书）。
- 还加了一条实用性判断：**区间跨度 ≥15 集 → 标「参考价值低」**。
  实测出来的：像「朝堂百官列班」「宫门禁军把守」这种通用镜头，模型给的是
  「第 5–40 集（可能出现在中段）」—— 照这个翻等于白翻。这类镜头根本不必抠集数，
  直接在素材库里找任意同年代宫廷空镜更省事。标出来就省掉了这个坑。
- 检索式跟着升级：带上「**剧名 第N集**」（如 `汉武大帝 第56集`）—— 整集上传在 B 站/YouTube
  很常见，搜到那一集、从推算位置往前翻，比关键词搜切片更稳。
- 实测（霍光第 1 期 6 处必须剪）：`s01`「托孤」→《汉武大帝》(2005，58 集) 推算**第 56–58 集、
  全剧 97%–100%、集尾**，自检确认存在；同一条里模型还给了《汉武帝》(1996) 和《霍去病》(2020)，
  **被自检判为「不确信存在（可能是编的）」并标了出来** —— 这三道加固是有用的。
- 验证：回归 456 → **473 项全过**（+17：区间解析的各种写法、进度由代码算、
  与总集数矛盾的两级处置、集内位置非分段词丢弃、过宽区间标记、检索式带集数）。

### 2026-09-16 · 取景单：让模型先告诉你「这场戏去哪部剧里找」
- 起因：阶段 1.5（剪素材）原来完全是人力活 —— 清单只说「要什么画面」，没说「去哪找」。
  霍光第 1 期六条必须剪的镜头里，第一条「汉武帝病榻前把周公负成王图交给霍光」就得靠剪辑师
  自己想起是哪部剧、哪一集、哪一分钟。
- 新增 `src/hsg/sources.py` + 命令 `run.bat agent --stage sources`（阶段 1 也会顺带产出）。
  「素材需求.md」里多一节**取景单**：每个必须剪的槽位给出候选片源（含主演/平台锚点）、
  **可复制的检索式**、回填栏（片源/集数/起止时间）、以及**预填好槽位的 import 命令**。
- **分工（对齐本项目三层约定）**：模型给候选片源与检索关键词（它的知识面）；
  代码给检索式拼装、槽位号、场景槽位列表、防编造兜底（确定性）；
  边界是**绝不自动绑槽位** —— 用谁的片子是版权与创作决策，永远走 `import_clip.py --slots`。
- **为什么坚持不给集数/时间码**：模型对这类确定性信息会编。提示词里禁掉、正则
  （`FABRICATED`）兜底把 `第58集`/`12:30` 换成占位句，只保留**剧情阶段**描述
  （「汉武帝晚年、大结局前后」）。集数与时间码由剪辑师在正片里确认并回填。
- **实测发现模型会编「剧名」**：第一轮它给了《霍光传奇》(2020)、《西汉演义》(2015),
  还标 high/mid 置信度；换一轮同样的槽位，《西汉演义》年份变成 2010、主演拼成
  「吕良伟、寇振海、王姬」。这类编造比空手更糟（剪辑师照着搜一无所获）。
  三道加固：① 提示词禁止编剧名、**每部剧必须给主演或平台锚点**，给不出的渲染成
  「很可能是编的」；② 增加一次**剧名自检**调用（`_selfcheck`）让它自己交叉核对，
  判 unsure 的在表里标出；③ 表头写死「以你能不能搜到为准，不要以本表为准」。
  加固后重跑：编造剧名消失，模型对找不到的槽位直接**不给候选**（只留检索式）。
- **顺带讲清方法**：找素材的实际动作是「拿检索式去 B 站/YouTube 搜，搜到了再拖进度条」，
  比「先查集数再找片源」快得多。所以检索式是主力，候选片源只是线索。
- 途中查出三个真 bug：
  ① **候选片源整体错位一格** —— 给模型的清单用 `enumerate(slots)`（0-based）编号，
     回填却按 `idx-1` 取：槽位 1 的候选写的是槽位 2 的画面。剪辑师会拿着第 4 场的画面
     去搜第 5 场的剧，且**完全看不出哪里错了**。
  ② `--stage sources` 顺手重建了需求清单，把已看过的描述/标注词冲掉（「遗诏托孤」→「托孤」）。
     改成复用现成清单，只在没有清单时才重建。
  ③ 演示用的 `stage_sources` 里 `from .llm import LLM` 被自己的补丁删掉了（NameError）。
- 环境限制（如实记）：这台机器上**核实不了剧名** —— 中文维基超时、百度返回验证页
  （491 字节 + `wappass`）、豆瓣/百科 302 鉴权、DuckDuckGo 超时、Bing 结果页渲染不出来
  （还返回了上一次查询的缓存）。所以「候选是否真实存在」只能靠人工在 B 站搜。
  这也是把「未核实」写死在表里的原因，不是偷懒。
- 验证：回归 424 → **456 项全过**（+32：检索式拼装/防编造正则/编号对齐/降级路径/
  锚点标记/上限/插进 .md 的顺序）；真实跑通 6 处必须剪的取景单。

### 2026-09-16 · 新增 `docs/生产线操作手册.md`（每期照着走）
- 起因：要开工做《古代十大权臣》第 1 集（霍光），「按什么顺序、敲哪些命令」散在
  README §3.8、`命令速查.md`、`Agent应用架构.md` 三处，而且都只讲片段。
- 新增 `docs/生产线操作手册.md`：定位成**操作层**（README §3.8 讲设计理由，架构文档讲实现，
  这份只讲怎么做）。内容：开工前 30 秒自查 → 阶段闸门总览 → 第 0~4 步逐条命令与判据 →
  素材硬线表 → 霍光第 1 期的实例（真实路径 + 6 条必剪槽位 + 可照抄的 import 命令）→
  排查表（挑错期/内存/覆盖度不动/占比超限/语音缓存 miss）→ 一句话总纲。
- 写文档时**核对了实现**，改掉两处我自己写错的地方：① 原来写「7 秒分镜切 4 段」——
  7 秒根本不用切，实际是 26 秒分镜 / 槽位上限 8.5 秒 → 切 3~4 段；
  ② 把 **8.5 秒（槽位切分上限 `needs.slot_seconds`）** 和 **10 秒（素材单段合规红线
  `clips.max_seconds`）** 挑明了，两个数混起来会以为清单要求超红线。
- 还补了一条容易当成两期的坑：`_素材需求.md` 用**选题标题**，`_EDL.json` / `_素材出处.md` /
  成片用**成片标题**（模型写稿时拟的，EDL 那份截前 28 字）—— 霍光这期就是
  「霍光：一个臣子凭什么能换掉皇帝？」vs「霍光废帝：…」，同一期。
- README §3.8 顶部与 `命令速查.md` 顶部各加一条指针，避免第二份口径漂移。

### 2026-09-15 · 阶段 2 会挑错脚本（静默渲染错的一期）+ 系列视图看不出 L1
- 起因：要开始做《古代十大权臣》第 1 集（霍光），先核实「怎么操作」时的顺序 ——
  发现 `data/output` 下躺着 3 份 `*plan*_metadata.json`（前期验证留下的），
  而 `--stage 2` 默认走的是 `find_latest_metadata`，它 glob 的是**所有** `*_metadata.json`。
  也就是说：中间只要跑过别的期（或用过 `rerender` 写过新的 metadata），
  阶段 2 会**静默渲染错的那一期** —— 出来的片子一切正常，内容却不是你要的那集。
- 修法：新增 `agent.pick_plan(cfg, explicit)`，优先级 **显式 `--metadata` > 最新 plan 产物
  > 兜底最新 metadata**；`stage 2` 的日志从「脚本 <文件名>」改成两行：文件名 + 这一期是什么
  （新增 `agent.meta_brief()` 读 metadata 里的 `series`/`series_ep`/`topic`）——
  只报文件名，人根本看不出是哪一集。
- 多份脚本在场时（现在就有 3 份）`status` 不再只显示最新一份，而是摊开清单并标出
  「▶ 最新，就是它」；`stage 2` 也会 warning 出完整清单 + `--metadata` 的钉住写法。
  只有一份时不刷提示（没人愿意每次跑都读一段清单）。
- 另一个缺口：`run.bat series` 的进度表只打 L2（标题+描述），**看不出每集走哪一路** ——
  两级结构在系列视图里被压成了一级。现在每集跟 `［L1 类型］`，表头多一行
  「类型分布（L1）：政变与权力 4 / 官府与吏治 3 / …」。（系列片连着几集同类型是正常的，
  系列本来就窄；随机选题的「避开最近类型」只管随机池，`next_episode` 不走 `pick()`。）
- 验证：回归 415 → **424 项全过**（+9：`pick_plan` 的优先级、plan 更旧也要挑 plan、
  `plan_candidates` 排序、`meta_brief`、多份/单份的提示、系列视图的 L1）；
  `run.bat smoke-clips --small` **31 项全过**。

### 2026-09-15 · 系列片支持（`--series` 按集号取下一集）+ 冒烟脚本两个 bug
- 需求来自「想出一个古代十大权臣系列」。原来系统里**没有系列这个概念**：一类选题就是一集，
  「这是第几集、系列叫什么」只能手写进标题里。做 10 集就会踩三个坑：
  ① 集号靠标题手写，迟早写乱；② 自动选题会把系列打乱（第 7 集先出、第 3 集还没做）；
  ③ 成片和封面看不出这是系列（观众不知道自己在看第几集）。
- 系列落在用户选题池里，不新增文件格式：条目多两个字段 `series` / `ep`，
  `data/topics_user.json` 顶部多一个 `series` 段放系列说明（这个系列讲什么）。
  用 `python scripts\add_topic.py --series "古代十大权臣" --series-desc "…" --ep 1 --type … --title … --desc …`
  一集一集加（工具会拦下「缺集号」和「同集号重复」，退出码 2）。
- 关键约定：**系列条目不进随机池**（`UserPool.items()` 过滤）—— 系列是有顺序的，
  混进随机选题就没有系列可言。取下一集只有一条路：`--series "系列名"`（或者
  `run.bat agent --stage 1 --series "系列名"`），按集号升序 + 跳过生成记录里做过的题。
  `run.bat series "系列名"` 看进度（每集 ✓ 已出 / · 待做）。
- 系列的可见性有三处，少一处观众就看不出是系列（护栏 25）：① 成片与封面顶上的小字
  （新函数 `pipeline.kicker_text`，**成片和封面走同一个函数**保证口径一致，
  系列集显示「栏目 · 系列名 第N集」，单集照旧显示章节）；② `metadata`；
  ③ 生成记录（`history.build_record`）—— 进度统计靠它，不写进度永远 0/N。
  集号没填时只显示系列名，不硬编「第0集」。
- 一个量过的边界：封面顶部小字的 `_wrap_cjk(...)[:1]` 会把放不下的部分**静默截掉**
  （不报错）——「历史小故事 · 古代十大权臣 第1集」在竖屏 42px 字号下约 756px，
  可用宽 929px，一行放得下；测试里钉了两条（横屏/竖屏各一）。
- 写系列集的提示词里明确「这是系列第 N 集」，不让模型把别的集内容拿来充篇幅（护栏 27）。
- `run.bat series` 的进度表原来只打 L2（标题 + 描述），**看不出每集走的是哪一路** ——
  两级结构在系列视图里被压成了一级。现在每集后面跟 `［L1 类型］`，开头多一行
  「类型分布（L1）：政变与权力 4 / 官府与吏治 3 / …」，一眼看出有没有偏科。
  （系列片连着几集同类型是正常的 —— 系列本来就窄；随机选题那边的「避开最近类型」
  只管随机池，`next_episode` 不走 `pick()`，两条路互不干扰。）
- **冒烟脚本查出两个真 bug**（`scripts/smoke_clips.py`）：
  ① `check_true` **参数错位** —— 失败时写的是 `check(name, f"不成立 {detail}", True)`，
  而 `check` 的签名是 `(name, ok, detail)`：字符串被当成 `ok`（恒真 → **失败会被记成通过**），
  布尔值被当成 `detail`（`'  ' + True` 直接 TypeError）。后果是「任何一项断言失败 = 冒烟脚本崩掉」，
  看不到是哪一项、也看不到后面的检查。现在失败会老老实实打印 ✗ 和原因。
  ② `normalize_clip` 没传 preset（冒烟不是生产，改 `ultrafast`），跟上一轮测试的处置一致；
  新增 `--small`（960x540 / 540x960）让内存紧的机器也能跑完 —— 本机当时只剩 1.0GB 可用
  （13.7GB 的 92%），1080p 图层链直接 `Cannot allocate memory`。
  **--small 下唯一放宽的是 blurpad 那一项**（阈值 ×2.5 → ×1.8，实测比值 2.48）：
  低分辨率下编码块效应占比更大，而模糊衬底本就没细节、被块效应补回来的高频相对更多，
  比值天然变低（生产规格实测约 5.8×）。数字照打，严格标定仍按 1080x1920。
- 踩到的坑：`cfg.video.orientations[o]` 的值是 `Config`（dict 子类，靠 `__getattr__` 取
  `.width`），整个替换成普通 dict 会让渲染 `AttributeError`；就地改键才对。
- 系列初始化产物：`data/topics_user.json` 已有《古代十大权臣》10 集（霍光/曹操/司马懿/
  王导/桓温/宇文护/李林甫/严嵩/张居正/和珅），标题写成切口问句、描述只写范围不写数字结论
  （数字结论归写稿阶段的史实校验，池子里不许提前钉死）。
- 验证：回归 385 → **415 项全过**（+2：系列视图的 L1，后又加到 424）；`run.bat smoke-clips --small` **31 项全过**、
  真实规格 1080x1920 也 **31 项全过**（冒烟里那集现在就是系列集：顶部小字带的墨迹
  25920 → **103680 像素**，说明系列标签真画上去了）；blurpad 在真实规格实测 5.8×
  （阈值 2.5）；配置审计死配置 0 项；
  `--series` 取第 1 集、做过第 1 集后跳到第 2 集、无参数时系列不出现在随机池 —— 都实测过。

### 2026-09-15 · 回归测试在内存紧张的机器上也能跑了（`normalize_clip` 支持 preset）
- 现象：全量测试在 `test_clip_normalize` 崩掉，报 `x264 [error]: malloc of size 7088704 failed`
  —— x264 连 7MB 都分配不到。查下来是**机器内存吃紧**（14GB 总量只剩 1.4GB 可用：
  VS Code ×4 + opencode ×2 + chrome + Defender），跟代码无关（这条测试当天已通过十几次）。
- 修的姿势不是「跳过测试」，而是让它不那么吃内存：`clips.normalize_clip` 增加可选
  `preset`（留空 = x264 默认 medium，**生产行为一点没变**），测试用 `ultrafast`；
  `scripts/import_clip.py` 也暴露 `--preset`（批量导入提速，这些中间产物后面还要重编码一次，
  这里省下的质量没有意义）。
- 断言的东西一点没少（分辨率/帧率/去音轨/截断时长都与 preset 无关）。
- 顺带说明：这种失败**先怀疑环境再怀疑代码** —— 判据是「隔离跑同一条测试也失败」
  且「代码路径当天没改动」。测试（当时 385 项，现 413 项）全过。

### 2026-09-15 · 可以自己制定选题了：`scripts/add_topic.py` + 用户选题池
- 原来选题池是 `topics.py` 里的常量 —— **要加一条得改代码**，而「栏目做什么题」是你的判断，
  不该需要改代码才能表达。
- 新增 `scripts/add_topic.py`：`--type/--title/--desc [--mode]` 追加、`--remove` 删、`--list` 看、
  `--check` 体检（类型有没有描述、标题重复、描述里是否写了数字）。
  产物 `data/topics_user.json`（config: `story.user_topics`），**并进内置池**：
  `run.bat topics` 能看到、自动选题会挑到、`--type` 过滤、避开最近类型、查重全部生效。
- **类型可以自己新开**：`--type-desc "这一类讲什么" --type "近代交通" …`；
  类型仍是受控词表，写错当场列全部可选值。
- 工具里写死了两条写作约定并会检查：描述 35-60 字、**不在描述里写具体数字与结论**
  （那是写稿阶段史实校验的活，写进池子会一直传下去）。
- 测试 372 → **385 项**（当时）。

### 2026-09-15 · plan 产物文件名带上标题（同一天不再互相覆盖）
- 原来 `plan` 只写 `<日期>_plan_脚本.md` / `_metadata.json` —— 同一天跑第二次会**静默覆盖**
  第一期（踩过：试两次选题，第一期稿子没了，只能从备份恢复）。
- 改成 `<日期>_plan_<标题>_*.md`；`agent.find_plan_metadata` 的 glob 放宽成 `*plan*_metadata.json`
  以兼容旧命名（老文件照常能认出来）。
- 测试 369 → **372 项**。

### 2026-09-15 · 选题两级支持手动指定（`--topic-type` / `--topic-desc`）
- 之前 `-t` 只能给标题，L1 类型和 L2 描述都是模型判的 —— **没法自己制定两级选题**。
  新增两个 CLI 参数直接指定，配合 `-t` 使用。
- 类型是受控词表：**写错当场报错并列出全部可选值（退出码 2）**，不静默归到「未分类」——
  静默归类的后果是「避开连着做同一路」悄悄失效，而且很久之后才会发现。
- 手填的两级**不会被模型覆盖，也不白花一次调用**（`fill_levels` 已有值就不调 LLM）。
- 池子路径下 `--type` 与 `--topic-type` 一个意思（都当类型过滤）；指定类型但该类挑空了
  会明确报错而不是默默给别的类型。
- README §3.8 的流程扩成**五步 runbook**（定选题 → 出稿+清单 → 剪素材 → 出片 → 检查）。
- 测试 363 → **369 项全过**。

### 2026-09-15 · 提交信息精简 + 本更新记录建立
- 约定如上：commit 一句话，细节进 README。

### 2026-09-15 · README 与代码对齐（`4ef4518`）
- **两处说反了**：① 「BGM 默认关闭」→ 实际默认开，且三首按章节交替；
  ② 语速换算 4.60 → **4.70**，并补「校准必须按音色分组」。
- 配图链路写漏了最重要的一段：实际是**生成优先（image-01）→ 图库 → 兜底词 → 渐变底图**，
  原文只写了图库检索；补生成配图三要点（按分镜类型分派后缀、两条后缀禁「工笔/绢本」与「摄影」、
  控制台显示名「H3」API 不认）。
- Wikimedia Commons 从「不可用」改成「直连不可用、开代理后可用」（它还是切片料的首选源）。
- 新增 §3.8 影视切片路线整节；目录结构补 `docs/`；产物补 `needs/edl/clips/素材出处/bgm`；
  排查顺序补 `smoke-clips`。

### 2026-09-15 · 选题改成两级（`76ae3df`）
- **L1 故事类型**（受控词表 15 类，每类一句高层描述）+ **L2 详细标题与描述**，池子 62 条全部补齐。
- 类型不是装饰，它解锁三件事：选题**避开连着做同一路**（`pick(recent_types=...)`）、
  按类型挑题（`run.bat topics` / `--type`）、大纲知道往哪边展开
  （「夜里的城」按「生计与物价」写是宵禁下的生活成本，按「刑狱与流放」写就是违规的代价）。
- 两级都落到 metadata / `_脚本.md` 头部 / `history.json`；老记录按标题回查池子补类型
  （否则「避开最近类型」要等所有期重跑才生效）。
- 手填 `-t` 的题由模型判类型 + 生成描述，两步都可能失败 → 退回「未分类/空描述」，不阻断出片。
- 坑：`Topic` 位置参数顺序是 **(类型, 标题, 描述)**，写反会让整池 62 条数据错位（池子打出来是空的）；
  `desc` 里不许写具体数字与结论（那是写稿阶段史实校验的活，写进池子会一直传下去）。
- 测试 332 → **363 项全过**。

### 2026-09-15 · Agent 应用：阶段闸门 + AI 剪辑决策（`1607060`）
- 把生产线做成项目内的自主流程：**该停的地方停**（人工剪素材）、该 AI 决策的交给 LLM、
  确定性的活全归代码、干完必须交证据。
- 入口：`run.bat agent`（看卡在哪）→ `--stage 1`（脚本+素材需求清单，停下）
  → 人工导入素材 → `--stage 2`（AI 排镜头 → 渲染 → 自检 → 出片）。
  **阶段 2 绝不重写字稿** —— 人已照稿剪了素材，再改就是背刺。
- 新模块：`edl.py`（剪辑表 + AI 导演 + 合规闸门）、`frames.py`（抽帧/定格放大/像素验收）、
  `shotvideo.py`（镜头编码 + 多镜头拼接 + 贴回语音）、`agent.py`（编排+自检+素材出处清单）、
  `storyio.py`、`scripts/smoke_clips.py`、`docs/Agent应用架构.md`。
- 剪辑手法四种：`plain` / `slow_push` / `freeze_zoom` / `still_pan`。
  `freeze_zoom` 的取巧点：**抽一帧→裁块放大→当静态图走原静态图链路**，不写任何 zoompan 滤镜。
- 竖屏放横屏素材默认 **blurpad**（完整画面 + 上下模糊衬底），不硬裁（16:9 进 9:16 硬裁只剩中间 32%）。
- **冒烟测试查出 4 个真 bug**（都已修 + 钉住）：
  ① 片头段没口播时完全不带音轨，`concat -c copy` 会把整条成片的音轨全丢掉（成片静音）；
  ② 剪辑表把「同年代搜到的」素材顶替没剪素材的分镜 —— 替用户做创作/版权决策，
     切片占比被顶到 97.7%（红线 45%）；
  ③ 单段 10 秒上限本该只管切片引用，却对所有镜头生效，把 12.55 秒的静态图镜头报成红线；
  ④ 字幕滤镜吃不下绝对路径（`f=D:\...` 的反斜杠冒号被吞），顺带修 `make_silence`
     编码器跟扩展名走（AAC 塞 .mp3 直接失败且报错被吞）。
- 验证：回归 287 → **332 项**；`run.bat smoke-clips` **31 项全过**（合成假素材钉死全链路，横竖双版）。

### 2026-09-15 · 素材需求清单（T2.5，`fd8620f`）
- 工作流定死：**脚本 → 需求清单 → 按单剪素材 → 导入 → 渲染**。
  剪素材是最慢的人工环节，清单必须在剪之前独立产出（不能等 EDL）。
- 槽位粒度 = 画面切点（`s04_sh1`），素材粒度 = 一个分镜：22 秒分镜切 3 段，
  但**只有第 1 段要真剪**，第 2 段起标「↺ 复用本场素材」。优先级（★必须）只落在**章节开场**。
- 时长三级取值：实测音频 > metadata 记的实测秒数 > 按字数估。
  第 7 期按字数估会把 414 秒低估成 311 秒（那期用慢 36% 的克隆音色）。
- 顺带修语速校准：`calib_rate.py` 改用**音频缓存 key 反查音色**，验证不过的期排除并标注 ——
  旧版只读 metadata 的 `tts_spec`（9-14 才有），早期期全被当「未知」混着平均，
  把 4.70 拉低成了 4.59。
- 测试 230 → **287 项**。

### 2026-09-15 · 影视切片素材库（T1+T2，`c763d0e`）
- `clips.py`（索引/多维检索/规范化导入/合规配额）+ `scripts/import_clip.py`（去音轨、绑槽位）。
- 合规硬线成文并强制：单段 ≤10s、切片总占比 ≤45%、导入强制 `-an` 且**入库前再验一次**、
  每条素材必记片名/年份等来源字段（发布前出「素材出处清单」举证）。
- 真跑端到端：14 秒带音轨素材 → 导入 → ffprobe 独立核对产物无音频流、时长正好 8.000s。

### 2026-09-15 · 切片方案定稿（`4ef746f`）
- 三条素材路线实测结论：① 影视切片（观感最好、版权风险最高）② 公共领域真实影像
  （Commons 能下、许可干净，但中国古代没有动态影像，真实影像起点是 1898 年）
  ③ AI 生成（零版权风险，人物跨镜头不一致）。
  古代题材取「PD 绘画与文物为主 + AI 生成补缺口 + 动效让它动 + 关键奇观处极少量用切片」。
- 方案「影视定帧 + 图生图二创」否决：`image-01-live` 会把内容也换掉（瓷器照片重绘成古装女子插画）。
- 产出 12 个任务的实现计划 `docs/plans/2026-09-15-历史解说视频v1-影视切片.md`。

### 2026-09-15 · BGM 三首按章节交替（`acc9030`）
- 期内第 i 章用第 i%3 首，**两件事缺一听感就崩**：① 响度归一化（EBU R128 两遍法到 -18 LUFS），
  候选曲录制电平差 21 dB，不归一化低电平那首在 0.08 音量下等于静音；
  ② 按**有声区间**切片（`safe_window`），有的曲子开头 42 秒是无声铺垫。
  验收：扫 1 秒窗 RMS 包络，**静音洞必须为 0**（实测章间电平差 1.4 dB）。

### 2026-09-15 · 封面排版 + 配图按分镜类型分派后缀（`87de9e8`）
- 封面标题「整段优先占一行、靠缩字号放下」，副标题标点优先断行
  （修「夜里的城：宵禁之后出门会怎样？」被硬折、以及更早的「县/官」拆词）。
- 配图风格后缀按分镜类型分派（器物向 / 场景向）：不分派会把叙事分镜压成静物小品
  （第 7 期实测：要找坊市布局图给了干裂土地、要找巡夜兵丁给了灯笼、要找衙门审案给了一把西式法槌）。
- 位置类改动一律**按像素验收**（`ImageChops.difference` 重建背景层做差），不靠肉眼/视觉模型。

### 2026-09-14 · BGM 换成舒缓向（`2032a8f`）
- 候选 12 首按四指标实测（频谱质心 / >4kHz 衰减 / 峰-均差 / 包络脉冲数）选 **Meditation**
  （Mixkit 免费音乐，可商用免署名）。视觉模型判「最干净」的那首被数值推翻（脉冲 21/分 vs 1/分）。

### 2026-09-14 · 换回系统音色 + 语速重校（`4de52ec`）
- 克隆音色 `hsg_story_v2` 试听不佳 → 换回 `audiobook_male_1 @1.1`。
- 语速换算必须从**已生成期的逐分镜时长**反算，别用 `probe-tts` 的范文值
  （连续合成比流水线的分镜合成快一截）。

### 2026-09-14 · image-01 生成配图接入（`933842f`）
- 新 key 生效后接入生成配图；试听 A/B 固化音色选择。

### 2026-09-14 · 场景级英文检索词（`2c3ac2c`）
- 查出「18/18 配图全来自英文检索词、中文词 0 个起作用」的根因
  （clean 策略下 bing 不参与，中文词完全空转），改为场景级英文词 + 图库路线天花板实测。

### 2026-09-14 · 封面折行修正（`3e0e60f`）
- 副标题改标点优先断行，修掉「县官」被拆成「县/官」。

### 2026-09-14 · BGM 接入 + 封面 + 配图复核（`63ae770`）
- BGM 混音（volumedetect 验收）、封面图生成、配图复核工具 `scripts/review_images.py`。

### 2026-09-14 · 地基轮：配置审计（`b8389f8`）
- 新增 `scripts/audit_config.py`：找出「写在 config 但代码从不读」的假开关。
- 清掉 10 个假开关，其中一个是真 bug（`video.outro_card` → `video.outro`）。
- 文档与代码一致性对齐。测试 113 → 126 项。

### 2026-09-14 · 起点（`4634f34`）
- `baseline: 历史小故事视频生成器` —— 建立回退点（选素材 → 大纲 → 写稿 → 史实校验 →
  TTS → 配图 → Pillow 画面 + ASS 字幕 → ffmpeg 编码拼接 → 横竖双版 + 封面）。
