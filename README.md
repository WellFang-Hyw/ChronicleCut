# historical_story_gen · 历史小故事视频生成

给一个历史题材（或者不给我），跑出一条 5-10 分钟的视频。有**两条产出路线**：

**路线 A · 静态图（默认，一条命令到底）**

```
选素材 → AI 出分章大纲 → AI 逐章写口播稿（切成「分镜」）
      → 史实校验（程序化 + LLM 审校，不合格带问题重写）
      → TTS 配音 → 找历史配图 → Pillow 合成画面 + ASS 字幕
      → ffmpeg 编码分镜 → 拼接 → 横屏 / 竖屏两版成片
```

**路线 B · 影视切片（历史解说视频形态：人工剪素材 + AI 剪辑）**

```
run.bat agent --stage 1   → 脚本 + 分镜 + 素材需求清单（出完就停下，这是闸门）
        ↓ 人工：按清单剪 6-10 秒片段 → python scripts\import_clip.py 绑槽位
run.bat agent --stage 2   → AI 导演排镜头(EDL) → 按镜头渲染 → 自检 → 双版成片
                            画面 = 影视切片 + 定格放大 + 大字标注 + 人名条
```

两条路线共用同一套画面层（排版、字幕、BGM、封面、拼接都是同一份代码），
所以 B 路线里没找到素材的分镜会**自动回退**成 A 路线的静态图画面，不会卡住出片。
架构与分工见 `docs/Agent应用架构.md`。

一句话概括技术选择：**文本用 DeepSeek，语音用 MiniMax，画面用 Pillow 离线合成，
运动与编码交给 ffmpeg**。

> 🤖 让 AI agent 在本项目干活前，先读 **`AGENTS.md`** —— 那里写了硬性约定
> （文本模型锁、配音固定用哪个音色和语速、改代码的护栏、已知机制局限）。

---

## 1. 快速开始

依赖已经装好了（`.venv` + 项目已 editable 安装）。

```bat
cd /d D:\Resources\code\historical_story_gen

run.bat                                           :: 随机题材，出横竖两版
run.bat -t "赤壁之战：一场大火如何改写三国"          :: 指定题材
run.bat --minutes 9                               :: 目标 9 分钟
run.bat plan -t "安史之乱"                          :: 只写稿（几毛钱，不出视频）

:: 影视切片路线（解说视频形态）
run.bat agent                                     :: 看生产线卡在哪、下一步干什么
run.bat agent --stage 1 -t "曹操为何杀吕伯奢"        :: 出脚本 + 素材需求清单，然后停下
run.bat agent --stage 2                           :: 素材到齐后：AI 排镜头 → 渲染 → 自检
run.bat smoke-clips                               :: 零 API 冒烟：切片 + EDL 剪辑链路

:: 选题（两级：故事类型 + 标题与描述）
run.bat topics                                    :: 看整个选题池
run.bat topics --type "刑狱与流放"                  :: 只看某一类
run.bat --type "行旅与驿传"                         :: 只在某一类里挑题
```

不想用批处理也可以：

```bash
.venv/Scripts/python.exe run.py -t "鸿门宴上，项羽为什么没杀刘邦"
```

> 从 Hermes 这类会注入 uv `PYTHONHOME` 的终端里跑，前面加
> `env -u PYTHONHOME -u UV_INTERNAL__PYTHONHOME`；`run.bat` 里已经清掉了。
>
> 完整命令表见 `命令速查.md`。

---

## 2. 目录结构

```
historical_story_gen/
├── AGENTS.md              给 AI agent 的项目约定（硬性约定/护栏/已知局限）← 先读这个
├── config.yaml            所有可调参数（时长、音色、图源、字幕、编码…）
├── README.md / 命令速查.md 说明与命令表
├── run.bat / run.py       入口
├── docs/
│   ├── Agent应用架构.md    切片路线的三层分工（AI 决策 / 代码执行 / 证据）与阶段闸门
│   └── plans/             实现计划（历史解说视频 v1：影视切片 + 剪辑标注）
├── src/hsg/
│   ├── config.py          配置加载 + 密钥三路兜底 + 文本模型锁
│   ├── models.py          Story / Chapter / Scene 数据结构
│   ├── textfmt.py         口播清洗（去 markdown/来源词、日期不补零、折行修正）
│   ├── llm.py             OpenAI 兼容的 LLM 客户端（JSON 模式 + 截断自动重试）
│   ├── material.py        素材检索（Bing 摘要，作人名/年份锚点）
│   ├── topics.py          选题池（两级：故事类型 + 标题与描述，可按类型挑）
│   ├── outline.py         阶段 A：分章大纲
│   ├── writer.py          阶段 B：逐章写稿 + 长度自适应扩写/精简
│   ├── verify.py          史实校验（规则层 + LLM 审校 + 锚点核查 + 复检闸门）
│   ├── tts.py             MiniMax T2A（+ edge-tts 兜底）
│   ├── images.py          配图：版权安全图库优先（Cleveland CC0 等）+ 年代匹配 + 去重
│   ├── clips.py           影视切片素材库：索引 / 多维检索 / 规范化导入 / 合规配额
│   ├── needs.py           素材需求清单：分镜 → 槽位（「要剪什么片段」的工作单）
│   ├── edl.py             剪辑表（EDL）：AI 导演排镜头 + 合规校验 + 素材出处
│   ├── frames.py          抽帧 / 定格放大 / 标注位置按像素验收
│   ├── shotvideo.py       镜头编码 + 多镜头拼接 + 贴回语音（护栏 12 的落地点）
│   ├── agent.py           阶段闸门编排：stage 1 停下等素材 / stage 2 自动出片
│   ├── subtitles.py       ASS 字幕（按分镜时长铺时间轴）
│   ├── media.py           Pillow 合成画面（背景层 + 前景层，两块分开出图）
│   ├── video.py           ffmpeg：编码片段 / 拼接 / 混 BGM / 读回参数
│   ├── pipeline.py        编排 + 长度控制 + 产物输出
│   └── cli.py             命令行
├── scripts/
│   ├── smoke_video.py     零 LLM 的媒体链路冒烟测试 ← 媒体出问题先跑它
│   ├── test_verify.py     零成本回归测试（清洗/校验/复检/版权策略/查重/分镜编号/语速传导/切片/需求清单/剪辑表/标注/两级选题，361 项）
│   ├── probe_clean_images.py  零成本配图专项探测（clean 策略下的命中率/版权/年代）
│   ├── rerender.py        从已有 metadata 重渲染（复用文稿和语音，只换配图/画面）
│   ├── check_layout.py    量一帧里标题带和字幕带是否重叠（不靠肉眼）
│   ├── smoke_clips.py     零 API 冒烟：切片 + EDL 剪辑链路（合成假素材钉死全链路）
│   ├── make_needs.py      出素材需求清单（脚本 → 槽位工作单；脚本先行那一环）
│   ├── import_clip.py     导入影视切片（规范化 + 去音轨 + 登记 + 绑槽位）
│   ├── calib_rate.py      从已生成期反算真实字/秒（校准 chars_per_second，别用 probe-tts）
│   ├── clone_voice.py     用参考音频克隆音色（MiniMax voice_clone → 可复用的 voice_id）
│   ├── tts_preview.py     多音色 A/B 试听（同一篇稿子各出一条 mp3，不跑 LLM/配图/渲染）
│   └── audit_config.py    配置审计（找出代码从不读的假开关 / 配置里没写的键）
└── data/                  产物（见「产物位置」）
```

---

## 3. 每个阶段在做什么、为什么这么做

### 3.1 素材（material.py）
拿 Bing 检索摘要当「人名/年份锚点」。**不是**让模型照抄，而是给它一个参照，
写稿仍以模型自身知识为主，再靠 `verify.py` 兜住事实。

为什么不用维基百科 —— 实测（2026-09，国内网络）：

| 来源 | 实测结果 |
|---|---|
| `zh.wikipedia.org` / `commons.wikimedia.org` | **直连**连接超时；**开代理/TUN 后可用**（实测 2026-09-15） |
| `baike.baidu.com` | HTTP 403（人机校验） |
| `cn.bing.com`（网页/图片检索） | **可用** ← 采用 |
| `api.artic.edu`（芝加哥艺术博物馆开放接口） | 可用，公共领域 |
| `collectionapi.metmuseum.org`（大都会 CC0） | 可用 |
| `openaccess-api.clevelandart.org` | 超时 |

### 3.2 写稿：为什么要分成两次 LLM 调用
- **阶段 A（大纲）**：一次调用只出结构（章节标题、要点、目标秒数、配图检索词）。
- **阶段 B（写稿）**：每章一次调用，返回若干「分镜」，每段 70-120 字。
- 好处：输出短、不易截断；6 章可以 **4 路并发**（实测比单次大调用快得多）；
  长度控制有地方下手（章节数 / 每章秒数）。

### 3.2b 切入方式：小切口（默认）还是事件式

`config.yaml` 的 `story.angle_mode` 决定整期的内容视角 —— 选题池、大纲、写稿、审校
四处提示词会一起切过去：

| 模式 | 讲法 | 例子 |
|---|---|---|
| `small`（默认） | 从一个具体的日常问题切进去，再用史实把答案讲成故事 | 「流放三千里：被流放的人一路上靠什么活下来？」<br>「城破那一天：官员、商人、僧人、工匠各自的结局」<br>「官员出差：从京城到广州，走多久、住哪儿、路上花多少钱？」 |
| `event` | 直接讲一场战役 / 一次变法的来龙去脉（旧行为） | 「赤壁之战：一场大火改写了三国」 |

小切口模式有三个关键设计，**每一个都是实跑撞出来的**：

1. **大纲必须先给「史实锚点」（facts）** —— 每章至少一条可考事实；
   写不出来就说明这个角度立不住，要换角度。
   起因：第一版让模型自己回答「一天走多少里、一天几文」，它为了答得漂亮，
   直接编了「每天走五十里」「米一升折银五厘」还说成《大清会典》的规定 ——
   审校一次抓出 8 条编造的具体数字。
2. **写稿前有「锚点核查」闸门**（`verify.audit_facts`，由 `verify.fact_audit` 控制）：
   逐条判断锚点 ok / unsure / wrong —— ok 保留、unsure 降级标注、wrong 删除。
   降级后的锚点写稿时只能用模糊说法（「史料记载不一 / 没有明确规定」），
   不许引出处和数字。
   判定标准要分清两类：**制度层面的通行描述（学界共识）判 ok，只有精确数字
   和逐字引用才要求出处落实**。第一版标准过严，18 条锚点全判存疑，
   内容被抽空 —— 严不等于好，会把节目变成一堆「史料没记载」。
3. **写稿只许用已确认的锚点**，并且明确允许「没有记载」本身就是答案。
   实测这句话很管用：稿子里会出现「律例里并没有规定一天要走多少里，
   实际快慢全看路况、天气和押解的差役」—— 可信度和信息量都高于一个编出来的数字。

四轮 prompt 迭代的效果（同一题材）：

| 轮次 | 结果 |
|---|---|
| 只有小切口 prompt | 审校抓出 **8 条编造数字**（还都声称出自某会典） |
| + 大纲 facts 锚点 + 写稿只许用锚点 | 降到 **2 条**真错（年代张冠李戴、里程与实际不符），不再有编造出处 |
| + 锚点核查（标准过严） | 18 条全判存疑 → 内容被抽空 |
| + 核查标准分两类 | 确认 8 / 存疑降级 10 / 删除 0，成稿后审校 **0 条必修** |

### 3.3 分镜（Scene）是整个流水线的原子单位
一个分镜 = 一段口播 + 一张配图 + 一条字幕 + 一段语音。

这么切的原因：如果一章 68 秒只配一张图会显得很呆；而如果整章合成一段语音再
按时间切开对齐画面，切口很容易切在字中间。按分镜合成则天然是
「一段文字 = 一段语音 = 一张图 = 一片字幕」，边界严丝合缝，字幕和语音不可能错位。

配套的一个正确性细节：音频缓存的文件名是 `scene_003_<文本哈希>.mp3`。
只按分镜号命名的话，改写后（分镜号没变、文字变了）会复用到旧语音，
字幕和声音就对不上了。

### 3.4 史实校验（verify.py）—— 这是本项目最值钱的一环
两层：

**规则层（零成本）**：空/过短/过长分镜、跨分镜重复句子、占位符残留、
markdown 残留、清洗后仍出现的「百科/维基/百度」等来源词、
年份是否落在故事声明的年代区间附近。

**LLM 审校层**：让模型当「史实校对员」，对照年代区间与素材逐条挑错，
只报能确证的问题（宁可漏报不要误报）。命中的分镜带着「错在哪、应该怎么改」
重写一次。

实测效果（第一次真跑「赤壁之战」，18 个分镜）：审校抓出 **10 条真实错误**，
包括

- 「刘备南撤到江夏」→ 应为夏口（且与后文自相矛盾）
- 「周瑜任左都督、程普任右都督」→ 东吴当时是**左督/右督**，「都督」是后世官称
- 「孙刘联军逆江而上」→ 方向说反（联军是溯江西上迎击，曹军才是顺江东下）
- 「三国鼎立从此拉开」→ 赤壁之后只是**三分雏形**，鼎立正式形成于 229 年孙权称帝
- 「火攻后周瑜立刻拿下南郡」→ 南郡之战打到 209 年，时间线不准

这类错误提示词约束根本拦不住，必须靠一遍独立的审校。

**复检闸门（verify.final_check）**：审校发现问题 → 带纠正意见重写章节 →
**重写的结果必须再验一遍**（规则层 + 审校层各跑一次）。没有这一步，
「修没修好」全靠猜。

- 复检通过 → 正常进 TTS。
- 复检仍有必修问题 → 清单写进稿件的「⚠️ 待人工核对」一节 + metadata 的
  `unresolved`，日志显著提示；
  `verify.block_on_fail: true` 时直接中止（退出码 3），**有问题就不出片**。

实测：审校抓 10 条 → 改写后复检 0 条必修（鸿门宴）；城破那天抓 3 条真错 →
复检归零。没归零的会以清单形式交到人手上，不会被静默忽略。

### 3.5 配图（images.py）—— 版权安全优先

**第一原则是版权**（防止视觉中国式索赔），所以默认 `images.license_policy: clean`：
只用版权明确的图库。

一个分镜找图的**完整顺序**（这是现在的实际行为，别只记住图库那一段）：

```
① 生成（images.generate: true，模型 image-01）  ← 拿到就用，直接结束
② 图库检索（原检索词）                          ← 生成失败/关掉时才走
③ 图库检索（兜底检索词：时代 + 泛画种）          ← 实测能把「馆里没有具体题材」救回来
④ 渐变底图（fallback_bg）                       ← 都没有就出一张底色图，不阻断出片
```

生成优先的理由：**不受图库索引限制**（冷门题材馆里根本没有对应的图），
而且**没有第三方版权**。代价是人物长相跨镜头不一致，以及偶发伪汉字（下面有护栏）。

| 图源 | 版权 | 需要 key | 特点 |
|---|---|---|---|
| `cleveland` | **CC0 公共领域** | 不需要 | 克利夫兰艺术博物馆。带 `creation_date`，**能按年代匹配**；中国绘画收藏好，图片两三 MB，下载快 |
| `artic` | **公共领域** | 不需要 | 芝加哥艺术博物馆。带 date_start/date_end |
| `met` | **CC0** | 不需要 | 大都会艺术博物馆。可限定「亚洲艺术部」，但原图常有十几 MB，排在后面 |
| `unsplash` | Unsplash License（免费商用、免署名） | `UNSPLASH_ACCESS_KEY` | 现代摄影，历史题材基本用不上 |
| `pexels` | Pexels License（免费商用、免署名） | `PEXELS_API_KEY` | 同上 |
| `si` | CC0 | `SMITHSONIAN_API_KEY` | Smithsonian Open Access |
| `rijks` | 公共领域 | `RIJKSMUSEUM_API_KEY` | 荷兰国立博物馆 |
| `bing` | **版权未明** | 不需要 | 全网关键词检索。**只在 `mixed` 策略下参与兜底** |

没配 key 的图源会自动跳过，不影响运行 —— 缺 key 不会让流程没图可用。

**实测（2026-09，国内网络直连）**：无 key 且**真的下得动**的公共领域源，目前只剩
`cleveland` 一家（met 的图服务器只有 20 KB/s，artic 的图床一律 403 —— 搜得到但下不动）。
Openverse 连接超时、美国国会图书馆被 Cloudflare 拦（403）、
Wikimedia Commons **直连**不可达（开代理后可用，而且是切片/叠加料的首选源 —— 上万条 PD/CC 素材）、
Rijksmuseum 旧 demo key 已失效。所以要扩充图源，最实际的是去领 Unsplash / Pexels
的免费 key（注册即得），填进环境变量即可，代码不用改。

**生成配图（image-01）的三个要点**：

1. **风格后缀必须按分镜类型分派**（`classify_scene_kinds`，规则兜底 + 一次便宜 LLM 覆盖）：
   器物分镜用「器物向后缀」（整幅画面只有器物本身），叙事分镜用「场景向后缀」
   （人物与场景为主体）。不分派的话器物后缀会把叙事分镜也压成静物小品 ——
   第 7 期实测：要找坊市布局图给了干裂土地、要找巡夜兵丁给了灯笼、
   要找衙门审案给了**一把西式法槌**。
2. 两条后缀都**不许**出现「工笔/绢本」（会诱发伪书法题跋 + 红印章）和「摄影」
   （会诱发图库水印）。这两个坑各踩过一轮。
3. ⚠️ **控制台里的显示名（如「H3」）API 不认**，配置里必须写模型名 `image-01`
   （文生图）/ `image-01-live`（图生图）。改配置时别照抄控制台。

选图顺序（**实跑调出来的**，仅在走图库那条路时生效）：

1. **年代贴合** —— 博物馆接口带年代字段，明末的故事优先选明代作品（陈洪绶 1598–1652
   这种），宋代山水差 470 年就往后排。这是用博物馆图源换来的额外好处。
2. **馆藏来源**（`prefer_page_keywords`：博物馆/档案馆/美术馆/古籍库）优先。
3. **美术标记**（画/卷/壁画/绢本/青铜/陶/俑…）优先。
4. **版权干净** 优先于版权未明。
5. **图源顺序**（同样贴合时克利夫兰/芝加哥排在 Met 前面，因为它们给的图小、下得快）。

两条必须的护栏：

- **文化圈过滤（`culture_filter`）**：博物馆接口是关键词匹配，版权干净也可能**离题** ——
  实测搜 `Ming dynasty painting city wall` 时，芝加哥艺术馆返回了《大碗岛的星期天》和
  伦敦议会大厦，大都会返回了波斯手抄本。讲明代城破的片子里放一张莫奈，比放现代照片
  还糟。所以默认只保留中国相关作品（`culture_keywords`），讲日本/欧洲故事时改这个词表。
- **单图体积上限（`max_mb: 25`）**：博物馆原图有几十 MB 的，画布最多 4K，超限直接跳过，
  否则一张图就能把流程卡住几分钟。

每条记录里都带 `round` 字段标明它是「生成」还是第几轮图库检索来的，
所以事后能看出这张图到底怎么来的。生成图的出处标成它的 `generator`（如 image-01）。

每张图选了哪张、什么许可证、什么出处，都写进 `data/images/_sources.json`
与稿件末尾的「配图出处与版权」一节 —— 公开被质询时这就是凭据。
版权未明的图会被显式标成 `⚠️版权未明`，不会混在干净图里看不出来。

想临时用回关键词检索（赶时间/自己判断风险）：`run.bat -t "主题" --license-policy mixed`，
或把 config 的 `license_policy` 改成 `mixed`。

背景：实跑搜「函谷关遗址 古关隘」拿到的是现代景区照片，画面里**有电线杆**，
历史沉浸感当场破功。上面这套排序是为此加的。

配套的质量措施：
- 分辨率门槛（默认 ≥700×480）、长宽比门槛、明显不适合当背景的 URL 直接跳过；
- **平均哈希去重**：同一张图不会在片子里出现两次；
- 每个分镜的检索过程写进 `data/images/_sources.json`（试了哪些词、多少候选、
  最终选了哪张、来自哪一组），没图时好排查。

**⚠️ 诚实说明这条链路的边界**：关键词检索没法保证「年代准确」。
排序优化能去掉大部分现代元素（景区照、素材图），但仍可能选到年代不对的历史照片
（实测秦汉语境下选到过一张民国时期的老照片）。要真正解决只有两条路：
① 把图源限制成 `["artic","met"]`（博物馆开放接口，公共领域、年代靠谱，但命中率低）；
② 接一个视觉模型做配图复核（需要视觉模型 key，当前项目没有，属可选扩展）。

博物馆接口的图是公共领域，Bing 搜来的图版权不明 —— 每个分镜的出处都会写进
稿件的「配图出处」段和 `metadata.json`。`images.show_credit: true` 可以把出处
显示在画面上（默认关）。要公开发布建议走 `artic`/`met` 那条路。

### 3.6 画面（media.py + video.py）
Pillow 出**两层**图，运动交给 ffmpeg：

- `bg.jpg` —— 配图按 `motion_scale`(1.16) 放大后铺满 + 压暗，**这层在 ffmpeg 里缓移**
- `fg.png` —— 透明层：上下压暗渐变 + 栏目名/章节标题/图注，**这层不动**

这样配图缓慢横移时文字纹丝不动，比整帧缩放耐看得多；运动用 `crop` 的
时间表达式实现（比 `zoompan` 便宜得多，20 秒片段只编 10 秒）。

**路线 B 的多镜头也走同一条链**：切片镜头是「背景层换成视频 + 标注层不变」，
定格放大则是「抽一帧放大后当静态图」，所以排版、字幕、BGM、封面、拼接全是同一份代码
（详见 §3.8）。

分段编码 + `concat -c copy` 拼接。转场是「淡黑」而不是 `xfade` ——
`xfade` 要全片重编码且滤镜链脆弱，淡黑在片段编码阶段就完成了，拼接是秒级的。

### 3.7 长度控制（pipeline.fit_length）
需求是 5-10 分钟，所以设了硬性区间 `[300s, 600s]`：

1. TTS 完之后立刻量总时长；
2. 在区间内 → 直接过（实测第一次就跑出 406.7s = 6.78 分钟）；
3. 超区间 → 按比例算出每章目标，挑偏差最大的章节带指令改写
   （扩写要求「补细节/场景/因果」，精简要求「砍重复/铺陈」），只重合成被改写的章节；
   最多两轮。

即临时长仍超出会明确告警，并指出该调哪个旋钮（`story.chapters` /
`seconds_per_chapter`）—— 不会静默给一条 4 分钟或 18 分钟的片子。

---

### 3.8 影视切片路线（解说视频形态）

静态图路线的问题是「一个 20 秒分镜就一张不动的图」。切片路线把画面换成**影视片段**，
再叠剪辑手法 —— 代价是素材得人工剪（v1 不做自动视频索引，那是一个独立工程）。

#### 完整流程（从定选题到出片，五步）

```
第 0 步 · 定两级选题（三种方式，任选）
  ① 让池子挑：   run.bat topics                   先看有哪几路故事
                 run.bat --type "行旅与驿传"        只在某一类里挑
                 run.bat plan                    不带 -t：自动挑（自动避开做过的 + 避开最近做过的类型）
  ② 池子里挑好标题后按它跑：run.bat plan -t "<池子里的标题>"   （类型和描述池子自带）
  ③ 自己制定二级：run.bat plan -t "标题" --topic-type "行旅与驿传" --topic-desc "这一期到底讲什么"
                  · --topic-type 必须来自受控词表（写错当场报错并列出全部可选值）
                  · 不填这两项就由模型判类型、生成描述（各一次便宜调用）

第 1 步 · 出脚本 + 素材需求清单（然后它会停下）
  run.bat agent --stage 1 -t "标题" --topic-type "行旅与驿传" --topic-desc "…"
  → data/output/<日期>_plan_脚本.md / _metadata.json
  → data/needs/<日期>_<题材>_素材需求.md（照这份剪素材）

第 2 步 · 剪素材并导入（唯一必须人工的一步）
  python scripts\import_clip.py --file <你剪好的.mp4> --id sg_01 --title "三国演义" \
      --year 1994 --people 曹操 --era 东汉末 --slots s04_sh1,s04_sh2
  随时看进度：run.bat agent（它会告诉你还缺几条、先剪哪几条）

第 3 步 · 出片（全自动）
  run.bat agent --stage 2
  → AI 排镜头(EDL) → 按镜头渲染 → 自检 → 横竖双版 + 封面 + 素材出处清单

第 4 步 · 检查
  run.bat agent              看状态与产物路径
  data/edl/*_EDL.json        剪辑表（每镜用了哪条素材的哪一段、什么手法）
  data/output/*_素材出处.md   发布举证
```

> 只想出稿不想出片：把第 1 步换成 `run.bat plan -t "…" --topic-type … --topic-desc …`
> （`plan` 与 `agent --stage 1` 是同一件事，后者会额外去出素材需求清单）。

**为什么必须停**：剪素材是整条链里唯一必须人工、也最慢的一环。
先剪后配会剪一堆用不上的，所以清单必须排在剪素材**之前**单独产出。
**阶段 2 故意不重写字稿** —— 你已经照稿剪了素材，它再「顺手优化」就是背刺
（时长跑出区间只告警，改不改由你定）。

#### 素材规范（导入工具会强制校验）

| 项 | 要求 | 为什么 |
|---|---|---|
| 单段时长 | **≤ 10 秒**（`clips.max_seconds`） | 合规：短段更像「引用」，长段更像「搬运」 |
| 画面占比 | 切片总时长 **≤ 成片的 45%**（`clips.max_share`） | 同上，超了**直接拒绝出片** |
| 音轨 | **必须没有**（导入强制 `-an`，且入库前再验一次） | 不用原声（版权），我们有自己的配音 |
| 画幅 | 1920x1080 / 30fps（工具自动转，`clips.spec`） | 拼接要参数一致 |
| 来源字段 | `title`/`year`/`people` 必填 | 发布前出「素材出处清单」举证 |
| 字幕水印 | 不要 | 我们自己叠标注，双重文字很难看 |

#### 清单怎么读（两级粒度别混）

清单按**槽位**给需求（`s04_sh1` = 第 4 分镜第 1 段），槽位是按「单个片段」切的：
22 秒分镜、单段上限 10 秒 → 切 3 段。但**每个分镜只有第 1 段要真剪**，
第 2 段起标「↺ 复用本场素材」（用同一段素材的不同段落铺满）——
所以一期通常是「几段到十几段要剪」，而不是几十段。优先级（★必须）只落在**章节开场**。

#### AI 导演与剪辑手法

阶段 2 会调一次 LLM 当**剪辑指导**，为每个分镜排镜头序列（哪条素材、从第几秒取、
取多久、什么手法、画面上打什么字）。它只能用手头真有的素材，编出不存在的 id 会被拦下。

| 手法 | 怎么实现 | 什么时候用 |
|---|---|---|
| `plain` | 素材直铺（素材本身就在动） | 交代环境、过场 |
| `slow_push` | 画布放大后按时间表达式缓慢平移裁切 | 想强调、放慢节奏 |
| `freeze_zoom` | **抽一帧 → 裁块放大 → 当静态图走原来的静态图路径** | 旁白说到某个瞬间/细节 |
| `still_pan` | 静态图 + 缓移（没素材时的回退画面） | 找不到素材的分镜 |

`freeze_zoom` 那个取巧是全案能低成本落地的关键：**不写任何 zoompan 滤镜**，
直接复用已有的静态图编码链路，还白捡原有的缓动。

画面元素：**大字标注**（2-4 字，带底条，手机小屏一眼读完）+ **人名条**（观众不认识这张脸时给）。
版面分区固定、按像素验收（`frames.ink_band`），不许互相侵入。

竖屏放横屏素材默认 **blurpad**（完整画面居中 + 上下模糊衬底），不硬裁 ——
16:9 进 9:16 硬裁只剩中间约 32% 宽度，人物很容易被裁掉。

#### 出片前的闸门与举证

- 剪辑表要过 `edl.validate_edl`：单段 ≤ 10 秒（**只管切片**，静态图镜头不受限）、
  切片占比 ≤ 45%、镜头总长 = 画面时长、素材 id 在库、取用区间不越界。
- 出片后自检：文件在、时长覆盖正文、画幅正确、**有音轨**（没声音这类问题日志里看不出来）。
- 自动出一份 `data/output/<日期>_<标题>_素材出处.md`：每条素材的片名/年份/人物/
  时长占比/用在哪些分镜 —— 公开发布被质询时这就是凭据。

#### 零 API 冒烟

```
run.bat smoke-clips        横竖两版都跑，用 ffmpeg 合成的假素材把整条剪辑链路钉死（31 项）
python scripts\smoke_clips.py -o landscape   :: 只跑一版，快一倍
```

素材库用**独立临时目录**，不会碰 `data/clips` 里你剪好的东西。

---

## 4. 模型选择（硬性）

### 4.1 文本：只用 DeepSeek

**文本内容只用 DeepSeek，MiniMax 只用于 TTS 与音色克隆。** 三层固化，不是靠注释：

| 层 | 机制 | 拦掉什么 |
|---|---|---|
| 启动 | `config.assert_text_provider()` 非 deepseek 直接抛错 | 有人改了 `config.yaml` |
| CLI | `--provider` 只接受 `deepseek`（argparse 直接退出 2） | `--provider minimax` |
| 运行时 | 每次运行打印分工横幅 | 「到底是不是在用 DeepSeek」的疑问 |

每次运行都会看到：

```
文本模型 (选题/写稿/史实审校) : deepseek / deepseek-chat
语音合成 (TTS)                : minimax / audiobook_male_1
```

要换文本模型：同时改 `src/hsg/config.py` 的 `TEXT_PROVIDER_LOCK` 和
`config.yaml` 的 `llm.provider`，两处必须一致。

### 4.2 语音：固定用系统音色 audiobook_male_1，语速 1.1

```yaml
tts:
  minimax:
    voice_id: "audiobook_male_1"   # 系统音色（默认）
    speed: 1.1
```

- 2026-09-14 曾换成克隆音色 `hsg_story_v2`，试听后认为听感不佳，当天改回系统音色。
  克隆流程本身可用，`hsg_story_v2` 仍挂在账号下：
  `data/base_voice/api-response_2.mp3`（15.87s 参考音频）经 MiniMax `voice_clone` 克隆而来。
- 克隆/换音频：`python scripts\clone_voice.py --check` 先验门槛，再
  `--voice-id <新id>`；工具会打印该往 config 里填什么。
- **换音色或改语速后必须重新校准 `story.chars_per_second`**，详见 §6。
  校准用 `python scripts\calib_rate.py`（0 成本，从已生成期的逐分镜时长反算），
  不要直接填 `probe-tts` 的范文值 —— 那个偏快。
- 想看两个音色的差别：`python scripts\tts_preview.py --script <脚本.md>
  --voice audiobook_male_1 --voice hsg_story_v2 --announce`。

---

## 5. 密钥（三路兜底）

`DEEPSEEK_API_KEY` / `MINIMAX_API_KEY` 本机在 **Windows 系统级环境变量**里，
正常不用配。但有些从图形界面启动的终端会把子进程环境清洗一遍（实测 Hermes 里
Python 读到的是空字符串），所以读取顺序是：

1. 进程环境变量
2. 项目 `.env`（参考 `.env.example`，会被 .gitignore 忽略）
3. **Windows 注册表**里的 Machine / User 环境变量（`src/hsg/config.py` 的
   `_registry_env`）—— 这是绕开清洗的那一层

三种途径任意一种有值就能跑，运行时会打印 `API Key：deepseek=已读取 minimax=已读取`。

---

## 6. 已知限制 / 需要注意

- **时长靠章节数而不是靠"目标时长"**。`story.target_total_seconds` 只用来算比例
  和打日志；真正决定内容量的是 `story.chapters` 和 `seconds_per_chapter`。
- **语速换算必须实测，而且必须跟上音色**。当前 `chars_per_second: 4.70` 是
  系统音色 `audiobook_male_1` + `speed=1.1` 的实测值（**只统计验证过音色的那 4 期**：
  72 段 / 8933 字 / 1900.0 秒）。
  ⚠️ 换算值要用 `python scripts\calib_rate.py` 从**已生成期的逐分镜时长**反算，
  不要直接填 `run.bat probe-tts` 的范文值 —— probe-tts 是连续合成，比流水线的
  分镜合成快一截（克隆音色那次：范文 3.91 / 实际 3.45；系统音色 4.77 / 实际 4.70）。
  ⚠️ **校准必须按音色分组，不同音色不能混着平均**：上一版这里的值是 4.59，多算了
  「白银、鸦片与钦差」那一期（4.14 字/秒）—— 那期用音频缓存名反查**任何候选都没命中**，
  根本不是这个音色录的。音色之间差得很远（克隆音色 3.45 vs 系统音色 4.70，差 36%），
  拿错值去估时长，成片长度和素材清单里的片段长度就全错了。
  `calib_rate.py` 现在会自动排除验证不过的期并标注。
  换音色或改语速后必须重算，否则初始估算偏、多花一轮改写 + TTS。
  参考：edge 的 `zh-CN-YunxiNeural` 实测 4.75 字/秒@1.0、5.22@1.1。
- **素材摘要质量一般**（Bing 摘要会混进游戏站、诗文站）。它的定位只是锚点，
  事实准确性最终靠审校层。
- **配图版权**：默认 `license_policy: clean`，只用 CC0/公共领域/免费商用图库
  （实测能用的只有克利夫兰艺术博物馆一家，见 3.5）。Bing（版权未明）只在
  `mixed` 策略下兜底。想扩充图源就去领 Unsplash/Pexels 免费 key 填环境变量。
- **TTS 兜底必须真的装上**。`tts.fallback_edge_tts: true` 只是代码路径 ——
  2026-09-11 真踩过：MiniMax 余额耗尽（错误码 1008），18 段语音全灭，
  而兜底报的是 `No module named 'edge_tts'`。已 `pip install edge-tts` 修好，
  现在 `run.bat --tts-provider edge` 可以用免费音色跑通全流程（试音/验证用）。
- **BGM 默认开着**（`bgm.enabled: true`），而且是 `bgm.playlist` 里**三首按章节交替**
  （第 i 章用第 i % 3 首），音量 0.08、开场白念完才淡入、循环铺满。
  两件事缺一听感就崩：① **响度归一化**（EBU R128 两遍法到 -18 LUFS）——
  候选曲录制电平差 21 dB，不归一化低电平那首在 0.08 音量下等于静音；
  ② **按「有声区间」切片** —— 有的曲子开头几十秒是无声铺垫，按 0 偏移切就是静音。
  换曲：把 mp3 放进 `data/bgm/playlist/` 并改 `bgm.playlist.files`；
  许可与实测指标记在 `data/bgm/来源与许可.md`。**不要用有版权的影视/节目配乐。**
- 渲染时间：一段 20 秒竖屏片段在 preset=medium 下约 10 秒编完，18 个分镜 ×2 个
  朝向 ≈ 6-8 分钟。想更快把 `video.preset` 改成 `faster`（实测差别不大）。

---

## 7. 生成记录与选题去重（不重复讲做过的故事）

每次跑完自动追加一条记录：

```
data/history.json     机器可读的完整记录（查重就靠它）
data/生成记录.md       人看的表格版（累计期数、时长、配图命中、校验情况）
```

看一下做过什么：

```bat
run.bat history                 :: 列出已生成的期目
run.bat history --backfill      :: 把 data/output 下已有的 metadata 补录进来
```

去重是两层的，因为「同一个故事换个说法」纯规则抓不住：

| 层 | 判据 | 成本 |
|---|---|---|
| 规则层 | 规范化后相同 / 一方是另一方子串（≥3 字）/ 二元组 Jaccard ≥ 0.55 | 免费 |
| 模型裁定层 | 只在「沾边但规则判不出」时调一次 DeepSeek，问「这两个题材是不是同一个历史事件」 | 一次便宜调用 |

实测效果：

  · `-t "鸿门宴"` → 规则层命中（子串），直接拦下，退出码 2
  · `-t "鸿门宴：项羽为什么放走了刘邦"` → 规则层判不出来（Jaccard 只有 0.19），
    模型裁定层判定「与已做过的《鸿门一宴，项羽为何放走刘邦？》是同一个故事」→ 拦下
  · 不带 `-t` 自动选题 → 自动跳过做过的题材（每次都会打印「已生成 N 期，本次已避开做过的」）
  · 选题池（小切口 30 条 / 事件式 32 条）全部做过之后 → 让 DeepSeek 出一个没做过的新题
    （出重了会重出，最多 3 次）

**选题是两级的**：L1 是**故事类型**（受控词表，每类一句高层描述说明这类故事讲什么），
L2 是**详细标题 + 与这个故事相关性最高的描述**。两级都会落进 metadata、
`_脚本.md` 头部与 `history.json`（老记录没有类型字段时按标题回查池子补）。
类型不是装饰，它让选题能**避开连着做同一路**（`pick()` 优先挑近期没做过的类型）、
能按类挑题（`--type`），也让大纲知道该往哪边展开。
看池子：`run.bat topics`（`--type "刑狱与流放"` 只看一类）。

被拦下时的提示：

```
ERROR  这个题材已经做过了：鸿门一宴，项羽为何放走刘邦？（2026-09-11，6.8 分钟）
         · 想换一个：去掉 -t/--topic，选题会自动跳过做过的题材
         · 确实要重做：加 --allow-duplicate
```

想去掉这个机制：`config.yaml` 里 `runtime.record_history: false`（记录仍可手动用
`history --backfill` 生成，但不再查重）。

记录里存了什么（`history.json` 一条的样子）：

```json
{
  "run_id": "20260911-114614",
  "generated_at": "2026-09-11T11:46:14",
  "topic": "虎门销烟：禁烟背后的账本",
  "title": "白银、鸦片与钦差：虎门销烟前的三本账",
  "chapters": 6, "scenes": 18, "chars": 1827,
  "speech_seconds": 442.0, "speech_minutes": 7.37,
  "video_seconds": {"portrait": 489.5, "landscape": 489.5},
  "images": {"total": 18, "found": 18, "fallback": 0},
  "verify": {"rule_fail": 0, "rule_warn": 0, "audit_issues": 3, "regenerated_chapters": [5]},
  "outputs": ["...\\_竖屏.mp4", "...\\_横屏.mp4"],
  "text_model": "deepseek/deepseek-chat",
  "tts": "minimax/audiobook_male_1"
}
```

---

## 8. 产物位置

```
data/output/20260911_<标题>_横屏.mp4        成片
data/output/20260911_<标题>_竖屏.mp4        成片
data/output/20260911_<标题>_脚本.md         口播稿 + 章节结构 + 配图出处
data/output/20260911_<标题>_metadata.json   全量元数据（分镜/时长/配图/时间轴/校验）
data/history.json                          生成记录（查重依据）
data/生成记录.md                            生成记录（人看的表格版）
data/material/                             素材缓存
data/audio/                                每个分镜的语音（文件名带文本哈希）
data/images/                               配图 + _sources.json
data/bgm/story_bed.mp3                     单曲 BGM（已启用）
data/bgm/playlist/*.mp3                     三首交替用的曲目（按章节轮换）
data/bgm/来源与许可.md                      曲名/作者/许可类型/实测指标（发布举证用）
data/slides/<朝向>/                         画面（*_bg.jpg 配图层 / *_fg.png 文字层）
data/segments/<朝向>/                       分镜片段 + _concat_*.mp4（拼接母版）

影视切片路线（路线 B）另外产出：
data/needs/20260915_<题材>_素材需求.md       剪素材的工作单（照着这份剪）
data/needs/20260915_<题材>_素材需求.json     同一份清单的机器版（出 EDL 时读）
data/clips/index.json                      素材库索引（每条素材的片名/年份/人物/绑了哪些槽位）
data/clips/norm/*.mp4                      规范化后的素材（统一画幅、已去音轨）
data/edl/20260915_<标题>_EDL.json           剪辑表（AI 排的镜头序列 = 第几秒用哪条素材的哪一段）
data/output/<日期>_<标题>_素材出处.md        发布举证：每条素材的片名/年份/占比/用在哪些分镜
```

---

## 9. 排查顺序（媒体出问题不要先怀疑流水线）

```bat
run.bat smoke          :: 零 LLM 跑通 配图→画面→字幕→编码→拼接（路线 A）
run.bat smoke-clips    :: 零 API 跑通 合成素材→绑槽位→排镜头→按镜头渲染→自检（路线 B）
python scripts\smoke_video.py --no-tts    :: 连 TTS 都不用，完全不花钱
```

`smoke` 通过说明媒体层没问题，问题在内容层（就先看 `plan` 的输出）；
`smoke` 不通过，报错信息能直接定位到是配图、Pillow 合成、ASS 还是 ffmpeg。
切片路线出问题优先跑 `smoke-clips` —— 它用 ffmpeg 合成的假素材、**不碰你剪好的素材库**。

切片路线卡住时先看状态（它会直接告诉你卡在哪一段、下一步敲什么）：

```bat
run.bat agent
```

判断版面（标题和字幕有没有打架）不要靠肉眼：

```bat
ffmpeg -hide_banner -loglevel error -y -ss 12 -i <成片> -frames:v 1 frame.png
python scripts\check_layout.py frame.png
```

---

## 10. 更新记录

> **约定（2026-09-15 起）**：git 提交信息**只写一句话**（说清这一轮干了什么就够），
> 详细改动、踩到的坑、验证数据全部记在**本节**。提交前先把细节追加到本节顶部。
> 为什么：commit message 是要在 `git log` 里一眼扫过去的，塞进十几行说明没人读；
> 而这些「为什么这么改」的信息对后面维护的人（包括 AI）很值钱，放这里才找得到。

### 2026-09-15 · 选题两级支持手动指定（`--topic-type` / `--topic-desc`）
- 之前 `-t` 只能给标题，L1 类型和 L2 描述都是模型判的 —— **没法自己制定两级选题**。
  新增两个 CLI 参数直接指定，配合 `-t` 使用。
- 类型是受控词表：**写错当场报错并列出全部可选值（退出码 2）**，不静默归到「未分类」——
  静默归类的后果是「避开连着做同一路」悄悄失效，而且很久之后才会发现。
- 手填的两级**不会被模型覆盖，也不白花一次调用**（`fill_levels` 已有值就不调 LLM）。
- 池子路径下 `--type` 与 `--topic-type` 一个意思（都当类型过滤）；指定类型但该类挑空了
  会明确报错而不是默默给别的类型。
- README §3.8 的流程扩成**五步 runbook**（定选题 → 出稿+清单 → 剪素材 → 出片 → 检查）。
- 测试 363 → **369 项全过**。

### 2026-09-15 · 提交信息精简 + 本更新记录建立
- 约定如上：commit 一句话，细节进 README。

### 2026-09-15 · README 与代码对齐（`4ef4518`）
- **两处说反了**：① 「BGM 默认关闭」→ 实际默认开，且三首按章节交替；
  ② 语速换算 4.60 → **4.70**，并补「校准必须按音色分组」。
- 配图链路写漏了最重要的一段：实际是**生成优先（image-01）→ 图库 → 兜底词 → 渐变底图**，
  原文只写了图库检索；补生成配图三要点（按分镜类型分派后缀、两条后缀禁「工笔/绢本」与「摄影」、
  控制台显示名「H3」API 不认）。
- Wikimedia Commons 从「不可用」改成「直连不可用、开代理后可用」（它还是切片料的首选源）。
- 新增 §3.8 影视切片路线整节；目录结构补 `docs/`；产物补 `needs/edl/clips/素材出处/bgm`；
  排查顺序补 `smoke-clips`。

### 2026-09-15 · 选题改成两级（`76ae3df`）
- **L1 故事类型**（受控词表 15 类，每类一句高层描述）+ **L2 详细标题与描述**，池子 62 条全部补齐。
- 类型不是装饰，它解锁三件事：选题**避开连着做同一路**（`pick(recent_types=...)`）、
  按类型挑题（`run.bat topics` / `--type`）、大纲知道往哪边展开
  （「夜里的城」按「生计与物价」写是宵禁下的生活成本，按「刑狱与流放」写就是违规的代价）。
- 两级都落到 metadata / `_脚本.md` 头部 / `history.json`；老记录按标题回查池子补类型
  （否则「避开最近类型」要等所有期重跑才生效）。
- 手填 `-t` 的题由模型判类型 + 生成描述，两步都可能失败 → 退回「未分类/空描述」，不阻断出片。
- 坑：`Topic` 位置参数顺序是 **(类型, 标题, 描述)**，写反会让整池 62 条数据错位（池子打出来是空的）；
  `desc` 里不许写具体数字与结论（那是写稿阶段史实校验的活，写进池子会一直传下去）。
- 测试 332 → **363 项全过**。

### 2026-09-15 · Agent 应用：阶段闸门 + AI 剪辑决策（`1607060`）
- 把生产线做成项目内的自主流程：**该停的地方停**（人工剪素材）、该 AI 决策的交给 LLM、
  确定性的活全归代码、干完必须交证据。
- 入口：`run.bat agent`（看卡在哪）→ `--stage 1`（脚本+素材需求清单，停下）
  → 人工导入素材 → `--stage 2`（AI 排镜头 → 渲染 → 自检 → 出片）。
  **阶段 2 绝不重写字稿** —— 人已照稿剪了素材，再改就是背刺。
- 新模块：`edl.py`（剪辑表 + AI 导演 + 合规闸门）、`frames.py`（抽帧/定格放大/像素验收）、
  `shotvideo.py`（镜头编码 + 多镜头拼接 + 贴回语音）、`agent.py`（编排+自检+素材出处清单）、
  `storyio.py`、`scripts/smoke_clips.py`、`docs/Agent应用架构.md`。
- 剪辑手法四种：`plain` / `slow_push` / `freeze_zoom` / `still_pan`。
  `freeze_zoom` 的取巧点：**抽一帧→裁块放大→当静态图走原静态图链路**，不写任何 zoompan 滤镜。
- 竖屏放横屏素材默认 **blurpad**（完整画面 + 上下模糊衬底），不硬裁（16:9 进 9:16 硬裁只剩中间 32%）。
- **冒烟测试查出 4 个真 bug**（都已修 + 钉住）：
  ① 片头段没口播时完全不带音轨，`concat -c copy` 会把整条成片的音轨全丢掉（成片静音）；
  ② 剪辑表把「同年代搜到的」素材顶替没剪素材的分镜 —— 替用户做创作/版权决策，
     切片占比被顶到 97.7%（红线 45%）；
  ③ 单段 10 秒上限本该只管切片引用，却对所有镜头生效，把 12.55 秒的静态图镜头报成红线；
  ④ 字幕滤镜吃不下绝对路径（`f=D:\...` 的反斜杠冒号被吞），顺带修 `make_silence`
     编码器跟扩展名走（AAC 塞 .mp3 直接失败且报错被吞）。
- 验证：回归 287 → **332 项**；`run.bat smoke-clips` **31 项全过**（合成假素材钉死全链路，横竖双版）。

### 2026-09-15 · 素材需求清单（T2.5，`fd8620f`）
- 工作流定死：**脚本 → 需求清单 → 按单剪素材 → 导入 → 渲染**。
  剪素材是最慢的人工环节，清单必须在剪之前独立产出（不能等 EDL）。
- 槽位粒度 = 画面切点（`s04_sh1`），素材粒度 = 一个分镜：22 秒分镜切 3 段，
  但**只有第 1 段要真剪**，第 2 段起标「↺ 复用本场素材」。优先级（★必须）只落在**章节开场**。
- 时长三级取值：实测音频 > metadata 记的实测秒数 > 按字数估。
  第 7 期按字数估会把 414 秒低估成 311 秒（那期用慢 36% 的克隆音色）。
- 顺带修语速校准：`calib_rate.py` 改用**音频缓存 key 反查音色**，验证不过的期排除并标注 ——
  旧版只读 metadata 的 `tts_spec`（9-14 才有），早期期全被当「未知」混着平均，
  把 4.70 拉低成了 4.59。
- 测试 230 → **287 项**。

### 2026-09-15 · 影视切片素材库（T1+T2，`c763d0e`）
- `clips.py`（索引/多维检索/规范化导入/合规配额）+ `scripts/import_clip.py`（去音轨、绑槽位）。
- 合规硬线成文并强制：单段 ≤10s、切片总占比 ≤45%、导入强制 `-an` 且**入库前再验一次**、
  每条素材必记片名/年份等来源字段（发布前出「素材出处清单」举证）。
- 真跑端到端：14 秒带音轨素材 → 导入 → ffprobe 独立核对产物无音频流、时长正好 8.000s。

### 2026-09-15 · 切片方案定稿（`4ef746f`）
- 三条素材路线实测结论：① 影视切片（观感最好、版权风险最高）② 公共领域真实影像
  （Commons 能下、许可干净，但中国古代没有动态影像，真实影像起点是 1898 年）
  ③ AI 生成（零版权风险，人物跨镜头不一致）。
  古代题材取「PD 绘画与文物为主 + AI 生成补缺口 + 动效让它动 + 关键奇观处极少量用切片」。
- 方案「影视定帧 + 图生图二创」否决：`image-01-live` 会把内容也换掉（瓷器照片重绘成古装女子插画）。
- 产出 12 个任务的实现计划 `docs/plans/2026-09-15-历史解说视频v1-影视切片.md`。

### 2026-09-15 · BGM 三首按章节交替（`acc9030`）
- 期内第 i 章用第 i%3 首，**两件事缺一听感就崩**：① 响度归一化（EBU R128 两遍法到 -18 LUFS），
  候选曲录制电平差 21 dB，不归一化低电平那首在 0.08 音量下等于静音；
  ② 按**有声区间**切片（`safe_window`），有的曲子开头 42 秒是无声铺垫。
  验收：扫 1 秒窗 RMS 包络，**静音洞必须为 0**（实测章间电平差 1.4 dB）。

### 2026-09-15 · 封面排版 + 配图按分镜类型分派后缀（`87de9e8`）
- 封面标题「整段优先占一行、靠缩字号放下」，副标题标点优先断行
  （修「夜里的城：宵禁之后出门会怎样？」被硬折、以及更早的「县/官」拆词）。
- 配图风格后缀按分镜类型分派（器物向 / 场景向）：不分派会把叙事分镜压成静物小品
  （第 7 期实测：要找坊市布局图给了干裂土地、要找巡夜兵丁给了灯笼、要找衙门审案给了一把西式法槌）。
- 位置类改动一律**按像素验收**（`ImageChops.difference` 重建背景层做差），不靠肉眼/视觉模型。

### 2026-09-14 · BGM 换成舒缓向（`2032a8f`）
- 候选 12 首按四指标实测（频谱质心 / >4kHz 衰减 / 峰-均差 / 包络脉冲数）选 **Meditation**
  （Mixkit 免费音乐，可商用免署名）。视觉模型判「最干净」的那首被数值推翻（脉冲 21/分 vs 1/分）。

### 2026-09-14 · 换回系统音色 + 语速重校（`4de52ec`）
- 克隆音色 `hsg_story_v2` 试听不佳 → 换回 `audiobook_male_1 @1.1`。
- 语速换算必须从**已生成期的逐分镜时长**反算，别用 `probe-tts` 的范文值
  （连续合成比流水线的分镜合成快一截）。

### 2026-09-14 · image-01 生成配图接入（`933842f`）
- 新 key 生效后接入生成配图；试听 A/B 固化音色选择。

### 2026-09-14 · 场景级英文检索词（`2c3ac2c`）
- 查出「18/18 配图全来自英文检索词、中文词 0 个起作用」的根因
  （clean 策略下 bing 不参与，中文词完全空转），改为场景级英文词 + 图库路线天花板实测。

### 2026-09-14 · 封面折行修正（`3e0e60f`）
- 副标题改标点优先断行，修掉「县官」被拆成「县/官」。

### 2026-09-14 · BGM 接入 + 封面 + 配图复核（`63ae770`）
- BGM 混音（volumedetect 验收）、封面图生成、配图复核工具 `scripts/review_images.py`。

### 2026-09-14 · 地基轮：配置审计（`b8389f8`）
- 新增 `scripts/audit_config.py`：找出「写在 config 但代码从不读」的假开关。
- 清掉 10 个假开关，其中一个是真 bug（`video.outro_card` → `video.outro`）。
- 文档与代码一致性对齐。测试 113 → 126 项。

### 2026-09-14 · 起点（`4634f34`）
- `baseline: 历史小故事视频生成器` —— 建立回退点（选素材 → 大纲 → 写稿 → 史实校验 →
  TTS → 配图 → Pillow 画面 + ASS 字幕 → ffmpeg 编码拼接 → 横竖双版 + 封面）。
